# Reveal: pinned for Python 3.11+ on Apple Silicon (arm64).
# Every wheel below was verified present on PyPI for macosx arm64,
# cp311-cp313, on 2026-07-10.
numpy==2.2.6
opencv-python-headless==5.0.0.93   # SIFT, MAGSAC++, ECC, DIS flow, video read
Pillow==12.3.0
pillow-heif==1.4.0                 # HEIC/HEIF decode, bundled libheif
rawpy==0.27.0                      # DNG / Sony ARW decode, bundled LibRaw
imageio-ffmpeg==0.6.0              # bundles a static ffmpeg binary (no brew)
