__pycache__/
/.idea
.pytest_cache/
/.venv/
/_reveal/
# agent operating layer — local session state
/.claude/.env
/.claude/claude-docs/NEXT_SESSION_PROMPT.md
/.claude/.ship-title
/.claude/BLOCKING_QUESTIONS.md
/.claude/ATTENTION.md
/.serena/
# agent artefacts — never let generated bulk reach a commit
/verify-shots/
/scratch/
/fixtures/*
!/fixtures/MANIFEST.md
/benchmarks/runs/
*.bak-*
.env.bak*
# OS
.DS_Store

# TR5 research probe weight cache (scripts/research/roma_probe.py; 1.6 GB, never committed)
/roma_torch_home/

# transitions.py --camera model: the depth model cache + manifest, written by `transitions.py warmup`
# (99 MB, near GitHub's per-file limit; ./setup.sh --learned rebuilds them)
/models/depth/
/models/DEPTH_MANIFEST.json
