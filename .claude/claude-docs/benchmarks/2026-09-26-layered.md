# Layered transitions sheet — 2026-09-26 (third session; the first probes of `TRANSITIONS.md §12`)

Every clip: 2 s (60 frames), canvas capped at 1920 px, the tool's quality basket on the 480-px proxy.
Run dir `benchmarks/runs/2026-09-26/layered/` (gitignored): `index.html` is the review page with the
three property boxes per clip, a pick and a note per pair; `<pair>/<variant>/` holds `transition.mp4`,
`strip.jpg`, `report.json`, and for the probes `layers.jpg` (each layer at the start of its window) and
three interior frames. What varies: `flat` and the anchored clip are the two clips the owner graded on
the theme-anchors page, copied unchanged; every `layered…` clip is rendered by
`scripts/research/layered_probe.py` from the score `scripts/research/scores/<pair>[_variant].json`
(layers cut by rules on the offline depth model and the photo, one action and one time window per layer,
composited back to front; `TRANSITIONS.md §12.1`). `step first/last` = mean difference in levels between
the first interior frame and A, and the last interior frame and B; the endpoints themselves are the photos.
The goal numbers were calibrated on crossfade-versus-closer verdicts and do not see an orchestration
(`§12.2`). Owner picks: pending as of 2026-09-26 (export `layered_picks.json` on the page, then
`scripts/research/layered_probe.py --out DIR --page-only --picks layered_picks.json`).

| pair | variant | class | method | edge_ratio | warping | feat_floor | laplace_floor | contrast_floor | dissolve_fit | motion_share | step first/last | wall s |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| mismatch_6 | anchors_falloff | B | mls-anchors+falloff | 0.7892 | 0.0088 | 0.1586 | 0.3402 | 0.6654 | 0.8025 | -0.0007 | — | 6.86 |
| mismatch_6 | flat | B | saliency-panzoom | 0.6723 | 0.0101 | 0.1281 | 0.33 | 0.6385 | 0.4921 | 0.0162 | — | 6.93 |
| mismatch_6 | layered | L | layered-score | 0.0105 | 0.01 | 0.0552 | 0.2412 | 0.1965 | 0.2257 | 0.2897 | 0.05/0.05 | 9.4 |
| mismatch_6 | layered_overlap | L | layered-score | 0.0132 | 0.01 | 0.0057 | 0.195 | 0.1925 | 0.2447 | 0.3041 | 0.06/0.05 | 9.5 |
| mismatch_4 | anchors_alpha | B | mls-anchors | 0.1336 | 0.0033 | 0.1706 | 0.6193 | 0.8136 | 0.0461 | 0.4484 | — | 8.2 |
| mismatch_4 | flat | B | saliency-panzoom | 0.164 | 0.0038 | 0.0847 | 0.4547 | 0.8057 | 0.1953 | 0.1385 | — | 6.35 |
| mismatch_4 | layered | L | layered-score | 0.1878 | 0.003 | 0.1076 | 0.5179 | 0.7131 | 0.2151 | 0.0029 | 0.11/0.23 | 13.2 |
| mismatch_3 | anchors_falloff | B | mls-anchors+falloff | 0.0564 | 0.0106 | 0.0507 | 0.4735 | 0.7115 | 0.0194 | 0.4219 | — | 10.13 |
| mismatch_3 | flat | B | saliency-panzoom | 0.0591 | 0.0178 | 0.0653 | 0.4573 | 0.71 | 0.0241 | 0.1886 | — | 9.09 |
| mismatch_3 | layered | L | layered-score | 0.0707 | 0.0081 | 0.4452 | 0.9137 | 0.9493 | 0.1945 | -0.0635 | 0.14/0.0 | 10.1 |

## Scores (layer · photo · action · window · share of the canvas)

- mismatch_6 `layered` (the owner's sequence, verbatim in DECISIONS 2026-09-26 late): sky (A, backdrop 0.00–0.72, share 0.781); stars (B, materialise 0.50–0.92, share 0.012); clouds (A, dissolve 0.08–0.66, share 0.550); buildings (A, exit down 0.30–0.78, share 0.219); trees (B, enter from below 0.55–0.95, share 0.214); ac_unit (B, enter from the top right 0.62–0.97, share 0.108)
- mismatch_6 `layered_overlap`: the same layers with overlapping windows (sky 0.00–0.65; stars 0.40–0.88; clouds 0.05–0.60 with a drift of (3 %, −2 %) and a zoom-out to 0.94; buildings 0.25–0.68 with a zoom-out to 0.92; trees 0.45–0.90; ac_unit 0.50–0.92)
- mismatch_4 `layered`: sky (A, backdrop 0.00–0.85, share 0.281); water (A, backdrop whose matte moves to B's water, 0.10–0.85, share 0.495); clouds_b (B, materialise by density 0.35–0.90, share 0.350); sun (A, move_to B's sun by the Gaussian map 0.15–0.75, share 0.009); band (A, the skyline, dissolve top-down while drifting 26 % of the height 0.15–0.78, share 0.220); band_b (B, materialise bottom-up 0.40–0.85, share 0.121)
- mismatch_3 `layered`: b_all (B, hold, the base); a_back (A, disparity < 0.3, dissolve by noise 0.05–0.55, zoom-in to 1.04, share 0.647); b_mid (B, disparity 0.35–0.70, materialise 0.35–0.80, share 0.321); a_people (A, 0.30–0.85, dissolve 0.30–0.75, zoom-in to 1.06, share 0.242); b_near (B, > 0.70, materialise 0.60–0.95, share 0.155); a_fence (A, > 0.85, dissolve 0.55–0.90, zoom-in to 1.08, share 0.110)

## Defects seen while building (by the agent's eye; `TRANSITIONS.md §12.3`)

The depth model's mattes needed photo refinement (darkness for the trees, a texture veto for a dome
above a roof, a hand polygon for the far tower); the moved sun leaves its wider glow behind and the
target's glow fades in before it arrives; the sinking skyline of mismatch_4 streaks at its tall left
shore and B's clouds arrive as blotches; mismatch_3 is a depth-ordered patch reveal, no content
transforms inside a layer.

## Owner picks (layered_picks.json, 2026-09-26 13:19 UTC; verbatim)

| pair | closer | note | boxes (one picture / transforms / nothing invented) |
|---|---|---|---|
| mismatch_6 | none | anchors_falloff and flat more or less same as in previous runs ( at least not worse ), layered variants added intneresting movements of clouds / front objects but they leave a lot of countours , afterimages and in general feel too direct and rushed , but i like how  you at least was able to isolate some objects and themes in layered variants, still very junky and crossfade is present in all variants ( although in anchors_falloff it is  rather subtle ) | anchors_falloff: no/no/yes; flat: no/no/yes; layered: no/yes/yes; layered_overlap: no/yes/yes |
| mismatch_4 | anchors_alpha | anchors feel more or less same as in previous runs ( at least not worse, somewhat smooth, still crossfade present ), layered variants in this example are very juny patchy dirty transitions , not useful at all , i feel like this is because for this example luma and colors vary between frames and also lots of distinct edges  | anchors_alpha: no/no/yes; flat: no/no/yes; layered: no box |
| mismatch_3 | none | anchors feel more or less same as in previous runs ( at least not worse, still crossfade present ), layered variants in this example are very juny patchy dirty transitions , not useful at all , i feel like this is because for this example luma and colors vary between frames and also lots of distinct edges  | anchors_falloff: no/no/yes; flat: no/no/yes; layered: no box |

Reading, pair by pair (the agent's, dated 2026-09-26; `TRANSITIONS.md §12.6`): on mismatch_6 both
layered clips are the first clips on record with "content transforms" ticked (0 of the 22 clips on the
anchors page and none earlier), "one picture" is ticked on none, and the named faults are contours,
afterimages, a direct and rushed pace, and a crossfade present in every variant. On mismatch_4 and
mismatch_3 the layered clips get no box at all and are "very junky patchy dirty": the noise-ordered
patch dissolves and materialisations (all of mismatch_3's score; mismatch_4's clouds and skyline band)
are rejected, with the owner's cause: luma and colours vary between frames and the layers have many
distinct edges.

## Round 2 on mismatch_6 (2026-09-26, fourth session; page `benchmarks/runs/2026-09-26/layered_r2/index.html`; `TRANSITIONS.md §12.7`)

Three 3-second clips from one score (`scripts/research/scores/mismatch_6_r2{,_L,_drift}.json`)
beside round 1's `layered` and `flat` (copied unchanged). The owner's targets, verbatim: "1) i see
afterimage for tree crown, distinct clouds, bottom buildings contours 2) keep 3s for now". Picks:
pending as of 2026-09-26.

| pair | variant | class | method | edge_ratio | warping | feat_floor | laplace_floor | contrast_floor | dissolve_fit | motion_share | step first/last | wall s |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| mismatch_6 | layered_r2 | L | layered-score | 0.097 | 0.0067 | 0.0 | 0.099 | 0.1178 | 0.0536 | 0.2898 | 0.28/0.05 | 14.7 |
| mismatch_6 | layered_r2_L | L | layered-score | 0.0965 | 0.007 | 0.0 | 0.099 | 0.1178 | 0.211 | 0.3165 | 0.28/0.05 | 13.6 |
| mismatch_6 | layered_r2_drift | L | layered-score | 0.0934 | 0.0071 | 0.0 | 0.099 | 0.1178 | 0.0502 | 0.3112 | 0.28/0.05 | 13.1 |

What varies: `layered_r2` keeps the clouds' own colour while they erode; `layered_r2_L` darkens
them in luminance with the night sky; `layered_r2_drift` also drifts them 4 % right and zooms them
4 %. Shared by all three: the trees carry their own sky between the leaves through a wide soft
region around a leaf-level core; the clouds are opaque wherever they exist and erode thin-first with
the sky under them filled from the clear sky; the skyline cut snaps to the roof edges and the vacated
band continues the interpolating sky; the residual holes are filled by a normalized convolution over
a per-band quadratic fit. Two runs byte-identical (md5 `cddde79e…`, `f3efeeef…`, `7934592a…`).
The round-1 "far tower" polygon enclosed sky and is gone.

mismatch_4 round 2 (Track D, the same page; `TRANSITIONS.md §12.7`, score `mismatch_4_r2.json`), beside
`flat` and `anchors_alpha` (your pick of the day) unchanged: the sun disc moves and the second sun takes
over on arrival, the skyline band with its masts slides down behind the water, B's band rises behind it,
the waterline slides, no cloud layer, no noise-ordered patch. Picks: pending as of 2026-09-26.

| pair | variant | class | method | edge_ratio | warping | feat_floor | laplace_floor | contrast_floor | dissolve_fit | motion_share | step first/last | wall s |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| mismatch_4 | layered_r2 | L | layered-score | 0.1724 | 0.002 | 0.1548 | 0.6364 | 0.8443 | 0.1753 | 0.0777 | 0.12/0.1 | 15.7 |

### Owner picks on round 2 (2026-09-26, 20:49 UTC; verbatim; `TRANSITIONS.md §12.8`)

| pair | closer | boxes (one picture / transforms / nothing invented) | note |
|---|---|---|---|
| mismatch_6 | `layered` (round 1) | `flat` no/no/yes; `layered`, `layered_r2`, `layered_r2_L`, `layered_r2_drift` yes/yes/yes | "I have picked layered ( although it is still very raw)  just to highlight that this is at least remotely looks like right direction ( at least for this pair) . Removing individual objects still is junky, like they are some cheap decorations in 2d scene with hard edges  , but at least you attempt to decompose the scene now, also i like how stars appear. weakest part still are clouds that dissolve in a very raw  and naive luma manner , i would expect them to dissolve more like real clouds would do in nature ( but again , NOT crossfade) . Also still all parts of the image that move , have hard distint edges that break immersion and move in very linear manner " |
| mismatch_4 | `layered_r2` | `flat`, `anchors_alpha` no/no/yes; `layered_r2` no/yes/yes | "I have picked layered_r2 ( although it is still very raw)  just to highlight that this is at least remotely looks like right direction ( at least for this pair) . moving individual objects still are junky, like they are some cheap decorations in 2d scene with hard edges  , but at least you attempt to decompose the scene now and attempt to make transtions between some parts, like skyline in this case and moving sun between the frames.  Clouds and sun reflections on water are  still crossfaded and we still have some rough edges and afterimages but it all looks more dynamic now. " |

The first "one picture" ticks on record (mismatch_6, four clips). On the agent's defect list: "pretty much accurate".
