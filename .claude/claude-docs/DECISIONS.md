# Reveal — Decisions Log

One line per meaningful change: what was decided, files touched, numbers measured, verification
tier (harness-tested / real-pair-VERIFIED / UNVERIFIED). **Append-only, absolute-dated.**
Supersede a past line with a newer dated line; never edit or delete old ones. Owner orders are
quoted VERBATIM ("Owner order (verbatim intent): …"), never paraphrased. Product decisions taken
before 2026-09-13 live in `HANDOFF.md §6` (27 rows; the file moved to `.claude/claude-docs/` on
2026-09-13) and are cited by number, not copied here.

Compaction (a move is not an edit): at every era close, and whenever the hot section passes ~100 KB
(≈25k tokens — it must fit one read; the boot hook warns), move the closed era BYTE-VERBATIM to
`DECISIONS_ARCHIVE.md` under a dated `## Moved <date>` divider, record the md5 + line/byte count of
the moved block here in a Compactions table, append a 10–30 line era digest in its place (every
caveat and number preserved), and re-seat the append marker under the heading. Proof of purity =
the removed line count equals the moved block; no dated line changed.

## Compactions
| Round | Date | Moved | md5 | Lines / bytes |
|---|---|---|---|---|

## Traps & gotchas (durable — promoted from `HANDOFF.md`, each naming its origin)
- `findTransformECC` maps TEMPLATE(A) coords into INPUT(B) coords (applied with `WARP_INVERSE_MAP`): seed with `inv(H)`, invert the result back. Cost a bug in the founding build (2026-07-10, `HANDOFF.md §3.2.6`).
- `scale_h` transport shipped inverted once; harness check 19 pins it with ground truth (2026-07-10, `§3.3`).
- rawpy applies the camera flip itself — re-applying EXIF orientation to RAW output double-rotates (2026-07-10, `§3.1`).
- Grayscale change detection misses equal-luminance repaints; per-channel max over normalized channels (harness 21 failure, 2026-07-10, decision 4).
- An unsigned edge detector, or a window spanning two parallel structures, fabricates 5–12 px of fake wobble; judge wobble as residual from a fitted quadratic with signed single-transition tracking (field defect #1, 2026-07-12, `§5a`).
- Inlier counts are not comparable across matchers (SIFT 458 → SSIM 0.566; learned 408 → 0.429); arbitrate on peripheral SSIM (2026-07-13, decision 23).
- Optional code that is never executed is a liability: the learned path shipped unrun for three releases behind a `try/except` returning `None` (2026-07-13, `§5b`).
- kornia's LoFTR derives stride and x-scale from the HEIGHT alone; non-multiple-of-8 inputs mis-scale keypoints (2026-07-13, `§5b`) — moot since LoFTR is out, kept as a class: check a model's input-shape contract before feeding it resized images.
- A float→uint8 `astype` TRUNCATES: every composited frame lands half a level low, and against a byte-exact pinned endpoint that is a visible step (0.8 levels at the last frame of a clean dissolve, hidden-cut detector 1.96 instead of 0.02). Round (`to_u8`) at every cast; the transitions harness pins it (checks 11, 20, 24; 2026-09-13, `transitions.py`).
- A review surface must say what varies. Eight time-sampled frames of one transition, shown as a strip with no labels, were read by the owner as eight variants to choose from (2026-09-13, `benchmarks/runs/2026-09-13/index.html`). Label each tile with frame and time, put the variants' differences on the page, and give the reviewer a pick control that produces a file the sheet can ingest.

## Recent
<!-- append below this line -->
- **2026-07-10 — v1.0.0 founding build** (backfilled from `HANDOFF.md`). Classical pipeline (SIFT/ORB + MAGSAC++ + ECC), optional learned fallback (LoFTR then), auto-gated residual flow, stdlib HTTP server on 127.0.0.1, embedded page, wipe/fade video via piped frames; 51 harness assertions. Decisions 1–10 in `HANDOFF.md §6`. harness-tested (Linux sandbox).
- **2026-07-10 — v1.1.0 matching modes** (backfilled). `MODES = {reshot, loose}` as a CFG overlay; 4-DOF similarity fallback and always-on learned matcher in loose; residual hard-off in loose. Decisions 11–13. harness-tested.
- **2026-07-10..13 — v1.2.0 → v1.3.0 field defect #1: wavy subject edges** (backfilled; exact day not recorded). Root-cause chain reproduced in harness 63–65 before fixing; three designs rejected empirically (feathered kill zone, distance-ramp attenuation, per-subject rigidification); final: DIS flow as samples only → robust 20-parameter cubic field, whole-field rejection over 6 work-px. Wobble 8.5 px → 0.09–0.12 px on the reproduction. Decisions 14–20. harness-tested; real-pair-VERIFIED on the graffiti-box pair (box edge 0.54 px RMS vs camera original 0.62).
- **2026-07-13 — v1.3.1 confidence rescored on geometry only** (backfilled). Pixel-similarity terms (absolute SSIM, peak sharpness) removed as scene-dependent in opposite directions; local-max boolean kept as a ×0.6 penalty. Decision 21. real-pair-VERIFIED (first real pair: sift, 458 inliers, 0.74 px rmse → 95).
- **2026-07-13 — v1.4.0 learned matcher first executed and replaced** (backfilled). LoFTR out (plain-HTTP weights, input-shape bug, untestable); DISK + LightGlue in; cross-matcher arbitration on peripheral SSIM; latent `max([])` crash fixed. Decisions 22–23. harness-tested (77–79).
- **2026-07-13 — v1.5.0 learned matcher made genuinely offline** (backfilled). `TORCH_HOME` pinned to `models/`; manifest-gated runtime that refuses to download; warmup runs a real forward pass and records sha256s. Decisions 24–27; harness 80–82 (all sockets patched to raise). Footprint 2 files / 52 MB. harness-tested.
- **2026-07-21 — Repo initialised on GitHub** (`sukhov-work/re-veal`, commits `e51b4e5` init, `344e4c4` cleanup): source, harness, docs and the 52 MB of weights committed as-is. No behaviour change.
- **2026-09-13 — Bootstrapped the operating environment** (bootstrap-project v2, Scaffold mode; profile: existing Python 3.13 venv repo, laptop-only + GitHub publish remote, design of record `HANDOFF.md` present, plan absent, Serena registered on the JetBrains backend, Codex present, fan-out 3). Laid down: `AGENTS.md` (contract) + `CLAUDE.md` import · `.claude/settings.json` (allow-list incl. `.claude/**` + `.serena/**`, `models/**` denied, hooks via `${CLAUDE_PROJECT_DIR}`) · hooks `session-start.sh` (verified, 0.7 s) + `pre-compact.sh` (UNVERIFIED leg) · `.codex/hooks.json` SessionStart (UNVERIFIED) · `.claude/conventions/{architecture,testing,naming,error-handling,verify,contracts}.md` · `/reveal` skill + `references/{audit-mode,review-agent,tracked-backlog (T1–T6),laws}.md` + `checklists/{README,docs}.md` · memory graph under `.serena/memories/` (core 3.2 KB, 8 files) · this log with 7 backfilled lines and 8 promoted traps · `NEXT_SESSION_PROMPT.md` (gitignored) · `EXPLORATION_PLAN.md` DRAFT (H1–H3 harness slices, E1–E3 exploration directions — owner ranks) · `audits/README.md` · `scripts/clean.sh` · `.gitignore` agent block; 3 tracked `.DS_Store` untracked. Kept `HANDOFF.md` at the root as the design of record (existing convention). Skipped: Phase F delivery artefacts (shape A), SessionEnd auto-ship (not asked), lint gate (none in repo — T6). Gate baseline: harness 82/82 in 62.7 s · check all OK in 18.5 s (this laptop, torch present). Owner order (verbatim intent): "i want to keep exploring additional capabilities there ( e.g use and improve algorithms for additional modes, video transitions generation etc so need to build proper harness around it first". [tier: Standard] [revert: commit] harness-tested.
- **2026-09-13 — Transitions engine, slice TR1: the deterministic core ships as a second single-file tool.** Owner order (verbatim intent): "we need to start building transitions feature/engine, alogside existing logic ( do not alter and regress it ) . First - analyze  follow investigation , research and impl plan from `.claude/claude-docs/transitions-research/impossible-0.1.0/impossible/README.md` and `.claude/claude-docs/transitions-research/impossible-0.1.0/impossible/RESEARCH-PLAN.md` . FYI - the whole `.claude/claude-docs/transitions-research/impossible-0.1.0` dir and all contents there are  an artifact from in-depth web research from fable agent and serve more as a reference and guidence ." Analysis of the artifact (Impossible v0.1.0, 12 modules + harness, built against exactly this repo state — its `REVEAL-BASELINE.sha256` matches all nine roster files today): its own harness passes 34/34 on this Mac in 4.5 s (scratch venv with `av`), its E1 bench passes its own gates without MPS (720p 0.086 · 1080p 0.226 · 2160p 0.728 s/frame, measured with Gate 1 running concurrently). Ported the deterministic tier into `transitions.py` (1,076 lines, nine `# ----` sections mirroring `reveal.py`: grammar with 7 presets, class A homography+DIS / class B saliency-or-MLS correspondence with forward-backward certainty, 1/4-res importance-weighted coordinate splat morph, time-varying Lab color path, quality basket on a 480-px proxy, frames streamed to imageio-ffmpeg, CLI `pair` + `check`) and `transitions_harness.py` (36 checks, 5.3 s, own numbering space). NOT ported, each by a repo ruling: the package layout and separate venv (single-file rule, invariant 7), PyAV (T8), RoMa-on-MPS and its runtime download (decision 9, decisions 24–27 → T11), the generative stubs (§5b lesson), clips/sequence (TR4). Defect found and fixed during the smoke run: every float→uint8 cast truncated (trap above). Numbers on a Reveal-aligned synthetic 1200×900 pair: class A, 214 inliers, median displacement 0.19 px, morph 3.1 s / wipe 1.6 s for 30 frames, `edge_ratio` 0.44 (was 1.83 before the fix). Roster: `harness.py` 82/82 (71.5 s, concurrent bench), `check` all OK, `reveal.py` and every roster file byte-identical (`git diff` empty). Verification: harness-tested; real-pair claims UNVERIFIED (`verification_blocked`, T4). Docs: `TRANSITIONS.md` (new design of record), `AGENTS.md`, conventions ×4, plan §TR, backlog T8–T11. Assumptions taken without the owner (overrule by a newer line): the module name `transitions.py` (the artifact says "Impossible"), the shared venv, no new dependency. [tier: Deep] [revert: delete `transitions.py` + `transitions_harness.py` and the doc rows; `reveal.py` untouched]
- **2026-09-13 — Ruling (agent, owner to confirm): the dense-flow fence is scoped to the alignment output.** `HANDOFF.md` decisions 16, 18, 20 forbid dense or masked residual flow applied to pixels in Reveal's residual stage, i.e. in `after_aligned.jpg` and the slider, because it bends subjects. A transition video is a different output whose purpose is to animate the change; the morph's dense field is the transition's motion, not an alignment correction, so `transitions.py` does not cross the line. Reveal's own pixels and metrics are untouched by construction (no import either way). The §5a lesson still applies as a RISK to the morph on real re-shots (DIS content-chasing inside the changed subject) and is measured by TR2, not assumed away. [tier: Deep] [revert: n/a — supersede by a newer line]
- **2026-09-13 — Ruling (agent, owner to confirm): transitions stay CPU-only and weight-free until a warmup exists.** MPS remains out (decision 9) for the transitions engine as for Reveal; the E1 numbers show the CPU path meets the research plan's own speed gates, so the torch/MPS splat port (research E6) is not pursued. Any learned dense matcher (RoMa, research E4) lands only behind Reveal's v1.5.0 shape: setup-time download, real forward pass, sha256 manifest, runtime refuses to download. Harness check 3 pins the current state (no network-capable or weight-loading import). [tier: Standard] [revert: a newer line]
- **2026-09-13 (evening) — Owner answers on the transitions rulings and the plan's five open decisions.** Verbatim: on the name, "leave it as `transitions` and record as official name"; on the fence, "dense-flow decision - ok"; on speed, "lets try to speed up all stages ( at reasonable degree ) so i can get results quicker but without compromising quality , as this part of the project is also a taste issue , i will still have to do a lot of manual tests and we will iterate"; on the plan, "1) speed up phases so we have results including potential models quicker 2) gitignored fixtures INSIDE project 3) At your discretion , i want to get to dense matcher upgrade and generative tier soon enough 4) direct to `origin main` all the time , no need for branches 5) migrate and adjust as needed"; and "also make sure to create a rule to start using /no-slop skill to sanitize all dev facing  output incluing this session's artifacts". The effects are the four lines below. [tier: Standard]
- **2026-09-13 (evening) — `transitions` is the official name; the dense-flow scoping is confirmed.** Identity fence: transitions harness checks 37 (persisted names pinned) and 38 (the research codename does not occur in `transitions.py`); Gate 1b is 38 checks. The fence row is in `conventions/contracts.md`. The scoping ruling of the morning (decisions 16, 18, 20 cover the alignment output only) is now an owner decision and a dated row in `HANDOFF.md §11`. [tier: Quick] [revert: n/a]
- **2026-09-13 (evening) — Speed: the deterministic tier stays on the CPU and gets slice TR7; model stages choose their device by measurement.** Supersedes the morning line "CPU-only and weight-free until a warmup exists" in one respect: for the dense matcher (TR5) and the generative tier (TR6) the device is decided per slice by a measured run against a CPU run on a fixture, with the field difference stated in px; decision 9 stands for Reveal; nothing downloads at runtime. Profile of a morph frame (M3 Pro, 1080p / 4K): total 0.160 / 0.689 s; color path 0.055 / 0.243 s; hole fill + mix + cast 0.049 / 0.213 s; two forward splats 0.040 / 0.159 s; two backward warps 0.017 / 0.071 s; correspondence once 0.36 / 1.51 s. First TR7 step shipped: the Lab and float32 copies of both endpoints are computed once per transition (`color_cache`) and the hole fill runs in place; frames are byte-identical (sha256 over all presets on the harness pair equal before and after); 1080p morph frame 0.163 → 0.151 s. TR7 is rank 1 with targets 0.10 s at 1080p and 0.45 s at 4K. [tier: Standard] [revert: commit]
- **2026-09-13 (evening) — Plan RANKED; fixtures inside the repo; no lint gate; the agent pushes to `origin main`.** Rank: TR7, TR2, TR5, TR6, then TR3 and TR4, then H1/H3/H2 and E1/E2 (`EXPLORATION_PLAN.md §Rank`). Fixtures: `fixtures/` at the repo root, `.gitignore` has `/fixtures/*` and `!/fixtures/MANIFEST.md`; the catalogue file exists with no row; backlog T4 is no longer blocked on a location, only on the first pair. Lint: none now, the agent's call under "At your discretion"; T6 DEFERRED to the first phase boundary. Push: Phase 4 ends with a commit on `main` and `git push origin main`, no branches, no PRs; recorded in `AGENTS.md §Workflow`, the `/reveal` skill and `mem:decisions/session_workflow`; never with a red gate. [tier: Standard] [revert: n/a for rulings; the ignore rule by commit]
- **2026-09-13 (evening) — Design docs moved under `.claude/claude-docs/`; prose gate added.** `git mv HANDOFF.md .claude/claude-docs/HANDOFF.md`, `TRANSITIONS.md` beside it; `HANDOFF.md §11` Amendments added (move, fence scoping, device rule); §10's "51 assertions" now points at §7, so backlog T7 is CLOSED; the `Edit(HANDOFF.md)` row left `settings.json` (covered by `Edit(.claude/**)`). Citations keep the short names; `AGENTS.md` says where the files are. Prose gate: `.claude/rules/prose.md`, `AGENTS.md §Workflow` and the skill's Phase 3 require the `no-slop` skill's pass on every dev-facing text before it is saved or shown; applied this session to `TRANSITIONS.md` (rewritten), the handover and this log's new lines; the morning's lines stay as written (append-only). [tier: Standard] [revert: commit]
- **2026-09-13 (late) — Unintended content in commit `39d2c67`.** Besides the owner-staged research artifact, that commit carries `.claude/skills/no-slop/SKILL.md` and `SKILL-DNA.md`: the Skill tool copied the global `no-slop` skill into the project and staged it when the skill was loaded at 21:12; I did not notice before committing. Content is byte-identical to `~/.claude/skills/no-slop/`, which links into the ecom KB repo; the text is generic prose guidance. Kept for now (the prose rule works on a fresh clone); backlog T12 holds the keep-or-remove question. Pushed history is not rewritten. [tier: Quick] [revert: `git rm -r .claude/skills/no-slop` in a new commit]
- **2026-09-13 (late) — First real fixtures: ten pairs from the owner; first real HEIC decode in both tools.** Owner (verbatim): "i actually went ahead and copient mulitple pairs of test images to fixtures, i divided them roughly into 2 groups : matched ( with at least some or a lot of similarity ( like boxes)  between matched_N_S and matched_N_F where S stands for start and F - for finish ) and mismatched, where images are quite far from each other in terms of content. These pairs are quite random - i may add more in future and add video clips etc. Also notice that some of images in HEIC extension , should support that as well just in case." Files are named `match_N_S/F` and `mismatch_N_S/F` (5 + 5 pairs, 18 JPEG + 2 HEIC). All 20 decode upright through the shared loader code (EXIF orientation 6 on `match_5_S` handled; the two HEIC files are 6048×8064 with an ICC profile, 1.9 s each). Reveal on `match_1` (HEIC): reshot passes, sift, 368 inliers, 0.74 px, confidence 95 — the first real HEIC through `reveal.py`, recorded in `HANDOFF.md §11`. Transitions on `match_1` / `match_2` (canvas capped at 1920 px): class A, 492 / 899 sparse inliers, median displacement 63.7 / 140.8 px, `edge_ratio` 0.23 / 0.12 on morph, endpoints 0/0. The sweep tool is `scripts/bench_transitions.py` (sheet.json, sheet.md, index.html with strips under `benchmarks/runs/<date>/`, gitignored). real-pair-VERIFIED for these claims; the full ten-pair sheet is the next line. [tier: Standard] [revert: n/a]
- **2026-09-13 (late) — TR2 first real-pair sheet: ten fixtures through both tools; the E2 routing and flicker gates pass; the owner's rating is pending.** `scripts/bench_transitions.py --out benchmarks/runs/2026-09-13 --reveal` (new, tracked; outputs gitignored under `benchmarks/runs/`): 60 runs, one at a time, M3 Pro. Reveal `align --mode reshot` at native resolution passes all five `match` pairs (sift; inliers 121–1059; rmse 0.67–1.01 px; confidence 90–97; loose never needed) and refuses all five `mismatch` pairs with the plain message. Transitions (canvas ≤ 1920 px): class A on the five matched, class B (saliency-similarity, 6–36 sparse inliers) on the five mismatched; `edge_ratio` ≤ 0.55 on morph / dissolve / flow-dissolve, 1.23–1.99 on snap-morph (by design); endpoints 0 / 0 everywhere; morph 1 s renders in 4.7–7.9 s. Eye notes on four strips in `.claude/claude-docs/benchmarks/2026-09-13-real-pairs.md`: the graffiti-box morph keeps straight edges; a non-rigid subject ghosts; the sun lands on the sun without anchors; the sun-to-galaxy morph shows class B's rectangular patch. Roster pair `match_4`: 481 inliers, 0.79 px here vs 458 / 0.74 in the sandbox — environment, not code (`HANDOFF.md §11`). Backlog: T4 CLOSED, T3 and T9 advanced. real-pair-VERIFIED for every number above; the usability rating is the owner's (E2: ≥ 6 of 10). [tier: Standard] [revert: n/a — measurement]
- **2026-09-13 (night) — Two more fixtures; the sweep is incremental; a certainty floor is proposed.** Owner: "also added mismatch_6 ( day to night) and mismatch_7 ( vaguely same city landscape )". `scripts/bench_transitions.py` now merges into an existing `sheet.json` (rows carry `run_at`), so `--only mismatch_6,mismatch_7` re-ran 12 jobs and kept the ten earlier rows. mismatch_6 (day → night, 1146×1528 vs 3056×4064): Reveal refuses; transitions class B with 0 sparse inliers, the saliency box visible mid-morph as on mismatch_5. mismatch_7 (same skyline, different skies, JPEG vs PNG): Reveal strict PASSES (sift, 78 inliers, 0.89 px, confidence 92, ECC no rho) although the owner labelled it a mismatch; transitions class A (70 inliers, homography sane) with mean certainty 0.053, the lowest on the sheet, and the morph tears the clouds into blocky patches: the DIS residual chases unrelated sky, the mechanism of Reveal's field defect #1. Proposed slice TR2b: a certainty floor below which class A morphs on the homography alone with an INFO line (match pairs measure 0.33–0.81); waits for the owner's rating. Sheet now 12 pairs, 72 runs; routing 11 of 12 as labelled; `edge_ratio` ≤ 0.79 on the smooth presets. real-pair-VERIFIED. [tier: Standard] [revert: n/a — measurement; the merge behaviour by commit]
- **2026-09-13 (night) — Owner's first review of the real-pair sheet; the review page rebuilt; plan re-ranked toward object-aware correspondence.** Owner (verbatim): "I went through benchmarks/runs/2026-09-13/index.html , so several important notes here. 1) If the idea was to let me judge different variations of each type of each case ( e.g match_1 - morph_1s , variants 1-8 ( if this is what you intended) ) then i need a way to pick best for each pair and case and also need to undestand the difference , what went into those 8 options . Next, some matches like match_1, match_4 and match_5 look promising, at least somewhat resembling nice dynamic  transition ( still missing accuracy and those interesing intermediate transformations of parts of image ). Match_2 suffers that person in frame is shifted sideways ( because before/after not perfectly aligned) and only then morphs. Match_3 - basically person from start of frame erased/ dissolved into person in end frame. I am not saying that you must finetune now all cases to accomodate for people or something else specifically, but such junky transition show how we miss to grasp some themes or objects or ideas about given frame and build flows around them even if they are quite detached both physically and conceptually from each other.  As for mismatches - no miracle here, almost all results are just naive  cross-dissolves with very simple frame movements animation , looks very generic ( but maybe this is intent for this phase )" The eight tiles were eight frames of one clip, not variants: the page now labels frames with time, plays each variant inline, explains each preset from `PRESETS`, and exports per-pair picks to `picks.json`, which `scripts/bench_transitions.py --render-only --picks` merges into the sheet (`--render-only` rebuilds the page from `sheet.json` without re-running). Diagnosis per remark is in the sheet (`§Diagnosis`): match_2 slides because the moved person is a 141 px residual on a background-aligned pair; match_3 crossfades because two poses share no consistent flow; class B is a similarity + crossfade by design in this phase. Rank revised under the owner's discretion grant: TR5 (RoMa accuracy) → TR9 (SAM 2 + DINOv2 object correspondence, anchor editor) → TR6 (generative, for intermediate transformations of parts) → TR7 (byte-identical speed levers only) → TR2b (folded into TR5) → TR3/TR4 → H/E. E2 gate ("≥ 6 of 10 usable") not met on this sheet; picks pending. [tier: Standard] [revert: n/a — record; the page by commit]
- **2026-09-14 — Owner answers: picks, canvas ruling, mismatch_7 intent, README, PyAV, backends, vendored skill.** Owner (verbatim): "done with the picks answers , find them here: benchmarks/runs/2026-09-13/picks_1-14-10-2026.json;  regarding `cover-cropping a portrait onto a landscape canvas`, for now lets rule that finish target ratio wins , also may want to add letterbox optional mode + find least common ration/area mode; regarding mismatch_7 - it is very roughly match , it is same part of the city but taken in completely different conditions , camera settings , zoom etc ( i.e. it was never intended to be aligned , and this is the point ); we will update readme when there are satisfying results; `T8 PyAV before slice TR4 ...` - ok ; For TR5/TR6: an explicit yes per backend - YES, this is personal project, fetch anything you need; keep vendored no-slop. Record and process everything above for next session work" Picks (file `benchmarks/runs/2026-09-13/picks_1-14-10-2026.json`, table and notes verbatim in `benchmarks/2026-09-13-real-pairs.md §Owner picks`): 3 of 12 usable — match_1 flow-dissolve 1 s, match_4 morph 3 s, match_5 morph 3 s; match_2, match_3 and all seven mismatches none; the research gate E2 (≥ 6 of 10) is not met. New findings from the notes: the class B frame border walks through the picture on every mismatched pair (defect T13 → slice TR2c, first in the queue); texture arrives from one side on match_4 and the boxes shift on match_5 (slice TR2d, certainty-weighted warp, measured against §5a); 1 s suits subtle movement, 3 s won on the box pairs; the plain dissolve is "junky" everywhere. Effects: README stays until "satisfying results"; T8 PyAV approved for TR4; blanket approval to fetch model weights and packages for TR5/TR6 at setup time (each still behind warmup + manifest + refuse-to-download, license recorded); T12 CLOSED, the vendored no-slop copy stays; T9 CLOSED. [tier: Standard] [revert: n/a — record]
- **2026-09-14 — Canvas policy: the finish photo's aspect ratio wins.** `common_canvas(a, b, max_long, policy)` with `finish` (default): the canvas has the FINISH image's aspect and is the largest such rectangle both images cover without upscaling (`w = min(wA, hA·r, wB)`, `h = w/r`, even); `common` keeps the previous rule (smaller width and smaller height). CLI `--canvas finish|common`, `TransitionSpec.canvas`, `report.json` `canvas_policy`. On the twelve fixtures at `--max-long 1920` the default changes four canvases (match_5 1518×1920 → 1438×1920; mismatch_2 730×800 → 602×800; mismatch_4 1920×1236 → 1920×1092; mismatch_6 1146×1528 → 1146×1524); the other eight are unchanged. Harness section J, checks 39–40 (finish ratio without upscaling; common rule kept; max_long cap; unknown policy refused) — check 40 was first written with a conditional expression that collapsed to one condition and was rewritten as a plain and-chain the same hour. Gate 1b 40/40. Letterbox and shared-area modes are slice TR13. The sheet was not re-run; the next sweep carries the new default. [tier: Standard] [revert: `--canvas common`, or a newer line making it the default]
- **2026-09-14 — Correction: no pick is "usable as-is".** Owner (verbatim): "Note:  `>Usable as-is: 3 of 12` : make sure to account for my specific  notes, they simply resable what i am looking for but very far from perfect." The 2026-09-14 line above and the sheet said "3 of 12 usable"; the picks mark the variant closest to the intent per pair, and the owner's notes on each are the requirements. Corrected in the sheet, `TRANSITIONS.md §6`, the plan (TR2 row and amendment), backlog T9 and the handover to "0 of 12 usable as-is; 3 closest-to-intent picks". Lesson for the record: a pick control needs a separate "acceptable as-is" answer, or the summary must not infer acceptance from a pick. [tier: Quick] [revert: n/a]
- **2026-09-14 (evening) — TR2c shipped: class B frames keep covering the canvas; defect T13 closed.** Mechanism, measured on the 2026-09-13 renders: the class B similarity moved the whole frame, and wherever the moved frame no longer covered the canvas the coverage-aware mix in `morph_frame` switched to the other endpoint, so a rectangle of one photo sat inside the other (mid frame at t = 0.5: mismatch_1 18.6 % of the canvas without B, mismatch_3 36 %, mismatch_4 27 % without A). Fix (`transitions.py` Correspondence section): `panzoom_field` gives each frame its own displacement, a zoom of `TCFG["classb_zoom"]` = 0.10 about its salient center plus a pan toward the other frame's salient center, scaled by the largest fraction that keeps the frame covering the canvas (A zooms 1 → 1.1 while it fades out, B settles 1.1 → 1; the byte-exact endpoints force that pairing). Method `saliency-panzoom`; `report.json` gains `pan_fraction [fA, fB]`; the INFO line names the zoom and both fractions. `similarity_from_boxes` and `_affine_to_disp` removed; the anchors path (MLS) is unchanged and can still show its edge. Cost: the pan is capped, so far-apart salient blobs mostly zoom (mismatch_3, centers 720 px apart: 0.06 / 0.06; mismatch_4's two suns stay 64 px apart at mid-transition, they met before). Harness section K, checks 41–43: a positive control shows the old whole-frame shape leaves 12 % uncovered and steps 56 levels; the field contract holds min coverage 0.81 at t = 0.25..1 with a far target panned 2 % and a near one 100 %; end to end on two flat frames with one blob each, saliency finds the blob within 5 px, coverage stays ≥ 0.86 and the mid-frame profile step is 4 levels. Mutation (pan clip removed) turned 42 and 43 red (coverage 0.00, 56-level step). Class A frames byte-identical (sha256 over all seven presets on the harness pair, before and after). Sweep re-run on all twelve fixtures (`benchmarks/2026-09-14-real-pairs.md`; page `benchmarks/runs/2026-09-14/index.html`): 60 runs exit 0, endpoints 0 / 0, routing unchanged; class B median displacement 46–122 px (was 232–889); `edge_ratio` on morph 1 s 0.13–0.66 (mismatch_6 0.66, was 0.52; its flow-dissolve 1.04, the only smooth-preset value above 1); the `finish` canvas default moved four canvases (match_5 → 1438×1920, 89 inliers, 84.6 px). CLI on mismatch_4 twice: identical report and mp4. Gates: Gate 0 clean, harness 82/82, transitions harness 43/43 (7.2 s), both `check`s OK; `reveal.py` untouched. Docs: `TRANSITIONS.md` §2.3/§5/§6/§7, `contracts.md` (method values, `pan_fraction`), `architecture.md`, `testing.md`, `AGENTS.md` count, `MANIFEST.md` class B rows, plan TR2c DONE, backlog T13 CLOSED; `scripts/bench_transitions.py` carries `pan_fraction` (backfilled at `--render-only`) and describes class B. The zoom value 0.10 has no source beyond this session's eye; the owner's picks on the re-rendered mismatch strips decide it. real-pair-VERIFIED for every number above; the look is the owner's call. [tier: Standard] [revert: commit; `classb_zoom` 0 gives a plain crossfade]
- **2026-09-14 (evening) — TR5 preparation: RoMa measured on the CPU in a scratch venv; nothing in the repo yet.** `pip install romatch` (0.1.2, MIT; pulls torch 2.14.0, torchvision 0.29.0, timm 1.0.29, kornia, poselib, wandb, h5py, albumentations, loguru, matplotlib) into the session scratchpad. `roma_outdoor(device="cpu")` fetches two files through `torch.hub.load_state_dict_from_url` into `TORCH_HOME/hub/checkpoints`: `roma_outdoor.pth` 445,647,516 bytes (sha256 c7a45c80d41ad788a63c641d1b686d7cb3f297f40097c6f4e75039889e5cc8ba) and `dinov2_vitl14_pretrain.pth` 1,217,586,395 bytes (sha256 d5383ea8f4877b2472eb973e0fd72d557c7da5d3611bd527ceeb1d7162cbf428, Apache-2.0); the VGG19 encoder is built with `pretrained=False`, so there is no third file. `match()` resizes both inputs to 560×560 for the coarse pass and 864×864 for the field, aspect ignored, and returns a symmetric warp (1, H, 2W, 4) in normalized coordinates; `get_autocast_params` disables autocast off CUDA and carries the comment "mps is not supported"; the package warns "Local correlation is not supported on non-Linux platforms". On match_4 at 1536×1920 (the canvas `prepare` builds), 12 threads, concurrent with the fixture sweep: 40.6–44.8 s per match over four runs, against 0.74 s for homography+DIS; certainty mean 0.553 with 62 % of pixels above 0.5; median displacement 9.9 px (DIS 15.1); median difference to the DIS field 0.57 px over the canvas and 0.20 px where both are confident (52 % of pixels). Reading: on the best-aligned pair RoMa and DIS agree within a pixel, so TR5's case has to be made on match_2, match_3 and match_5 (the pairs the owner called out) by warping error and the strips before any integration. Integration needs a decision on harness check 3 (no torch import in `transitions.py`); Reveal's shape is a lazy import inside the learned path plus a socket-patched offline proof. Weights location still proposed as `models/transitions/` with its own manifest (owner question open). Probe script `roma_probe.py` lives in the session scratchpad, untracked. UNVERIFIED beyond match_4; the timing is an upper bound. [tier: Standard] [revert: n/a — measurement]
- **2026-09-14 (late) — RoMa beats the DIS field on match_3 and match_5 by the basket and by eye; the owner lifts the speed gate for TR5.** Owner (verbatim): "run the RoMa probe on match_2, match_3 and match_5 before next session , also timings below 1-2  min per pair  is not that much issue for now , lets achieve best quality and then see if and how to optimize". Method: `scripts/research/compare_fields.py --prepare` writes each pair's 1920-px canvas and its homography+DIS field; `scripts/research/roma_probe.py` (scratch venv with romatch 0.1.2, CPU, 12 threads, weights cached) matches once per pair (symmetric, so both directions come from one call) and converts the 864×864 normalized warp to canvas-pixel displacement fields with RoMa's certainty as the weight; `--compare` renders morph 1 s (30 frames) from both fields through the unchanged `iter_frames` and writes the quality basket, mid frames and side-by-side strips to `benchmarks/runs/2026-09-14/roma/<pair>/` (gitignored). Timing alone, no other load: match_2 55.4 s (896×1920), match_3 37.2 s, match_5 34.8 s (1438×1920); model load 8.7 s from cache. Fields: match_2 median difference 0.5 px, 17 % of pixels differ by more than 5 px, RoMa certainty mean 0.43 (DIS consistency 0.81); match_3 9.2 px median, 58 % over 5 px, certainty 0.16 (DIS 0.41); match_5 22.1 px median, 63 % over 5 px, certainty 0.27 (DIS 0.32). Basket, DIS → RoMa: match_2 warping error 0.0068 → 0.0068, `edge_ratio` 0.16 → 0.19; match_3 0.0038 → 0.0035, 0.13 → 0.09; match_5 0.0211 → 0.0162, 0.24 → 0.21; endpoints 0 / 0 everywhere; render time unchanged (3.9–6.2 s). Eye, mid frame (`mid_dis_vs_roma.jpg`): match_3 with DIS smears the person across the umbrella (the owner's "disintegrates"); with RoMa the person stays one coherent, ghosted figure. match_5 with DIS bends the left box and tilts the right box's edge; with RoMa both boxes keep straight edges and stay in place while the paint crossfades (the owner's "boxes didn't shift"). match_2 looks the same with either field: the person slides because she moved, a TR9 case. Reading: RoMa's certainty is low exactly where the content changed, and the splat's importance weighting then lets that region dissolve in place, which is what TR2d was going to build by hand; with the DIS consistency weight the wrong content match is confident in both directions. Score so far: 2 wins (match_3, match_5), 2 ties (match_2, match_4) of 4 pairs; match_1 and mismatch_7 not yet run. Decision: TR5 integration is next (Deep tier; a session of its own): warmup + manifest + refuse-to-download, lazy torch import inside the roma path with harness check 3 rewritten as "no top-level network-capable import + socket-patched offline proof", `--dense roma|dis` with the default decided by the full sheet, CPU only until a measured MPS equivalence. Speed: the 35–55 s per pair stands, per the owner's order above; TR7 targets do not apply to the dense matcher. The two research scripts are tracked under `scripts/research/` (not part of either tool; the probe needs the scratch venv). real-pair-VERIFIED for the numbers; the "wins" are the agent's eye and the basket, the owner's eye is pending on the strips. [tier: Standard] [revert: n/a — measurement; the scripts by commit]
- **2026-09-15 — Owner review of the 2026-09-14 run: nothing accepted; the RoMa comparison is now real clips; the "certainty holds regions in place" reading of 2026-09-14 late is withdrawn.** Owner (verbatim): "regarding runs/2026-09-14 , i went through all and didn't notice much difference since first run ( at least it didn't get worse) , the only marginal improvement is mismatch_7 where you at least started to somewhat keep couple of focal buildings in between transitions , clouds are a mess though. other mismatches stayd more or less same  simple junky cross-dissolves  but at least you made it more subtle. Still none of the mismatch pairs can be accepted. Also same grading and remarks from first picks.json apply here for matches, as i said , if there are improvements they are marginal. For roma samples - create me full run with video transitions, it is very hard to judge low-res static frames". Facts against it: mismatch_7 routes class A and TR2c did not touch it; its five clips have the same basket as on 2026-09-13 and the morph 1 s mp4 has the same md5 (df5e516d4f0fcaaec215833e460af668) in both run folders, so the improvement seen there is not from code. TR2c's own outcome by the owner: border gone, motion "more subtle", still "junky cross-dissolves", E2 unmet; the match picks of 2026-09-14 stand. Mechanism corrected (measured on the fields): in the regions RoMa marks uncertain (certainty < 0.3; 84 % of match_1, 78 % of match_3, 66 % of match_5, 96 % of mismatch_7) RoMa's displacement departs from the pair's global motion by 9.3 / 15.7 / 24.3 / 350 px median against DIS 11.9 / 32.2 / 46.4 / 243 px; RoMa wins match_3 and match_5 by moving changed content half as far, and the certainty itself only weights splat collisions (`forward_splat`), it does not hold a region in place — on mismatch_7 (certainty 0.03, forward-backward error 227.7 px) the RoMa clip is worse (warping error 0.0120 → 0.0194). TR2d (displacement × certainty) is therefore a separate mechanism to measure, not "folded in". Delivered: `scripts/research/compare_fields.py --render` and `--page` (the probe now also writes `roma.json`); RoMa probed on match_1 / match_4 / mismatch_7 (46.4 / 46.4 / 46.1 s alone; certainty 0.13 / 0.55 / 0.03); 24 clips (six class A pairs × morph 1 s, flow-dissolve 1 s, snap-morph 1 s, morph 3 s) from the RoMa field through the unchanged encoder and basket, endpoints 0 / 0, each decoding to its frame count, on `benchmarks/runs/2026-09-14/roma/index.html` beside the DIS clip of the same variant with a verdict control that exports `roma_picks.json`; table in `benchmarks/2026-09-14-real-pairs.md §RoMa clips` and `TRANSITIONS.md §6`. Warping error DIS → RoMa on morph 1 s: match_1 0.0064 → 0.0070, match_2 0.0109 → 0.0121, match_3 0.0038 → 0.0037, match_4 0.0102 → 0.0080, match_5 0.0195 → 0.0176, mismatch_7 0.0120 → 0.0194; flow-dissolve `edge_ratio` rises with RoMa on every pair (0.23 → 0.64 on match_1), cause not established. No tool code changed; gates unchanged from 2026-09-14 (harness 82/82, transitions harness 43/43, both `check`s OK). real-pair-VERIFIED for the numbers; the verdicts are the owner's. [tier: Standard] [revert: n/a — measurement; scripts by commit]
- **2026-09-15 — Owner verdicts on the RoMa clips: RoMa is not adopted as a drop-in field (1 better, 1 same, 3 DIS better, 1 neither); the changed region needs a transformation, not a fade.** File `benchmarks/runs/2026-09-14/roma/roma_picks.json`, verbatim: match_1 "same" — "both ok-ish , can't see visible improvement with RoMa"; match_2 "neither usable" — "they look similar , both just do a lateral move as before ( see my complaint in previous picks file for this match_2 ) , roma did worse here as side edges become more distorted"; match_3 "roma better" — "This is one case where roma transition looks more natural (as if person grows, although still quite messy, especially person's edges)"; match_4 "dis better" — "roma does better transition for background here ( like wall and floor, more natural movement) , but box itself looks a bit boring as if prev image on it simply  fades out and new one fades in, no transformations ( although this may be useful as transition mode in our reveal app) "; match_5 "dis better" — "as in previous match_4, same issue - box itself looks a bit boring as if prev image on it simply  fades out and new one fades in, no transformations"; mismatch_7 "dis better" — "roma here look very unnatural , as if after image unwraps with some cheap 3d cloth effect with lots of artifacts and visible edges. Dis version partially suffers as well and both are unusable but it at least more promising". Owner order (verbatim): "record  and prepare for next session". Effects: plan TR5 (≥ 5 of 7 rule) fails at 1 of 6 and TR5 is not integrated; the quality basket's "wins" on match_4 / match_5 (warping error down, boxes straight) were read by the owner as the absence of a transformation, so the basket does not measure what the owner grades — a lesson for the record: the basket screens for defects, the owner's clip verdict is the quality metric. What survives: RoMa's background motion (match_4) and match_3; RoMa's low-certainty field is harmful (mismatch_7) unless scaled (TR2d). New slice TR14, changed-region transformation, is the next design pass (candidates in the plan row: TR2d floor, a designed deterministic effect inside the changed mask over RoMa's background field, the generative tier steered by the mask; "fade in place" kept as a named mode per the owner's aside). Rank: TR14 design → TR2d measure → TR6 measurement → TR9 → TR5 as a hybrid component. Recorded in `benchmarks/2026-09-14-real-pairs.md §Owner verdicts`, plan TR5/TR14 rows and amendment, handover, `mem:core`. [tier: Standard] [revert: n/a — record]
- **2026-09-15 — Correction to the line above, and the timing rule for experiments.** Owner (verbatim): "the quality basket scored "boxes stay straight" as a win and you scored it as boring :  here i simply was meaning  that content on the box faded in a boring manner, i liked how the box itself holds position.  Record that i am ok even with 3-5 min of claculations pair for tests, if that will be needed, in general no hard upper limit ( reasonably still) while we experimenting". Effects: the lesson in the line above is narrowed — the basket and the owner agree that the box holding its position (RoMa, match_4 / match_5) is a win; what no basket number sees is whether the changed content reads as a transformation or a fade, and that is what the owner grades. TR14's brief is therefore two requirements, not one: the changed object keeps its place (RoMa's field satisfies it; DIS does not) AND its content transforms between the endpoints (neither field does; the candidates are in the plan row). Timing: for experiments, 3–5 min per pair is acceptable and there is no hard upper limit within reason; recorded in the plan (TR5 row), `TRANSITIONS.md §7.1`, the handover's standing order and answered list. Corrected in the sheet's §Owner verdicts and the handover. [tier: Quick] [revert: n/a — record]
- **2026-09-15 — TR14 design written; TR2d measured and rejected as written; the changed region's floor is `hold`.** Probe `scripts/research/tr14_variants.py` (research only; imports both tools, neither tool changed): nine variants on match_3 / match_4 / match_5 / mismatch_7 at morph 1 s, plus 3 s on the box pairs, 66 clips with endpoints 0 / 0, page `benchmarks/runs/2026-09-15/tr14/index.html` (pick control exports `tr14_picks.json`), numbers in `benchmarks/2026-09-15-real-pairs.md`, design `TRANSITIONS.md §10`. The mask is Reveal's changed-region detector on the homography-aligned pair (match_4 0.276 of the canvas, match_5 0.49, match_3 0.189, mismatch_7 0.432); box-edge straightness is harness O's tracker at p90 in a ±10 px window following the camera motion (the photos themselves read 2.54 / 3.89 px on match_4). Findings: (1) TR2d as written (displacement × certainty) stops an uncertain region from following the camera: match_5's boxes move 2.9 px against 70 px of camera motion and detach from the wall; mismatch_7's sky freezes (0.4 px) while the skyline moves 330 px; the basket cannot see it (warping error falls 0.0176 → 0.0119). (2) The residual form `hold` = camera motion + (field − camera motion) × certainty keeps the object in place on every pair (in-mask displacement = the camera motion: 5.2 / 72.8 / 4.2 / 330 px), keeps the box edge at the photo's straightness (match_4 2.54 px against `dis` 6.3), and fixes mismatch_7 (warping error 0.0081 against `dis` 0.012 and `roma` 0.0194; no cloth effect). (3) Inside match_4's box the DIS consistency weight is confident on the wrong match, so a weight-free `hold-dis` gates the DIS residual with the photometric mask instead: close to `hold` but not equal (straightness 4.4 px, `edge_ratio` 0.136 at 3 s against 2.54 and 0.027) because the 8 px feather lets the residual act in the dilation band. (4) Three deterministic effects inside the mask over `hold` (`luma`: new content in order of the old content's brightness; `edge-grow`: outward from the new content's Canny edges; `melt`: a seeded curl-noise re-sampling × sin(πu)) change nothing outside the mask and leave the edge at the photo's straightness once the melt feather starts past the detector's 24 px dilation (`melt` 5.31 → `melt-soft` 2.42 px). (5) `hold` on mismatch_7 exposes the start frame's moved border as a rectangle at mid-transition (the class A twin of T13; DIS hid it by tearing): backlog T14, a canvas question. Recommendation (`§10.6`): build `hold` as the floor behind one spec option `changed` with values `flow` (today, default, byte-identical), `hold` (the owner's "fade in place" mode), `luma`, `edge-grow`, `melt`; the owner's picks choose the effect; five harness checks named with their mutations. Plan rows TR14 / TR2d / TR6 / TR5 updated; `TRANSITIONS.md §6` rows and `§7.1`. The owner's picks are pending; every number above is real-pair-VERIFIED, the look is the owner's call. [tier: Deep] [revert: n/a — design and research scripts; the tools are byte-identical (`git diff` empty on the four roster files)]
- **2026-09-15 — TR6 researched from primary sources and first-measured: SD 1.5 inpainting on MPS is the cheapest generative route for a masked region; LTX-2.3 through the MLX port is the only start+end-frame video route on this Mac.** Fact sheet by a research sub-agent (primary sources only; in the session scratchpad, digest in `TRANSITIONS.md §10.7`): Wan 2.2 does not document first+last-frame video and Wan 2.1 FLF2V-14B weighs 82 GB (out on 36 GB); DiffMorpher is S-Lab 1.0 non-commercial on SD 2.1-base, Framer BSD academic-only, DreamMover has no license file; RIFE (MIT) and FILM (Apache-2.0) are deterministic inbetweeners without Apple Silicon evidence; diffusers documents fp16 (not bf16) on MPS. Measured in a scratch venv (torch 2.14.0, diffusers, MPS fp16; `stable-diffusion-v1-5/stable-diffusion-inpainting`, creativeml-openrail-m, 2.0 GB, not gated): a masked SDEdit pass on match_4's `hold` mid frame at 512×640, strength 0.5, 20 scheduled steps (10 run), guidance 6: 1.43–1.62 s per step, 14.3–16.2 s per image, load 49.8 s including the download, peak RSS 2.41 GB, the same seed byte-identical (max abs diff 0), another seed 13.0 levels mean; the output is a coherent painted panel inside the mask with the wall untouched, and no temporal coherence across frames (a 30-frame clip would be 7.5 min and flicker). LTX-2.3 (Community License, `license:other`, not gated) through `dgrauet/ltx-2-mlx` (MIT; `keyframe --start --end`; int4 pack 59.7 GB of which the keyframe subset is 29.5 GB, plus Gemma-3-12B 4-bit 8.1 GB) was installed with `uv` and the subset fetched into the session scratchpad; the first `keyframe` run (match_4, 384×512, 25 frames, seed 0, `--low-ram`, twice) is the handover's first step if it did not finish in this session. Nothing in the repo; every weight stays outside it until a warmup + manifest exists (decisions 24–27). Owner order (verbatim, 2026-09-14): "YES, this is personal project, fetch anything you need". [tier: Standard] [revert: n/a — measurement]
- **2026-09-15 (late) — LTX-2.3 keyframe interpolation measured on this Mac: 6–7 min per clip, seed-reproducible, and the clip is a cut, not a transition.** Setup: `dgrauet/ltx-2-mlx` (MIT) with `uv`, the int4 pack `dgrauet/ltx-2.3-mlx-q4` subset (29.5 GB + the pre-fused distilled transformer 11.3 GB; LTX-2 Community License, `license:other`, not gated) and `mlx-community/gemma-3-12b-it-4bit` (8.1 GB), all in the session scratchpad, nothing in the repo. Three obstacles before a run: the port shells out to `ffmpeg` and the Homebrew binary on this Mac is broken (libx265 dylib missing; fixed with a PATH shim to the venv's imageio-ffmpeg 7.1 binary); `keyframe` refuses the distilled model ("hallucinates unrelated content") and needs `--dev-transformer transformer-dev.safetensors --cfg-scale 3.0` with `--distilled-lora ltx-2.3-22b-distilled-lora-384-1.1.safetensors`; `--low-ram` needs `transformer-distilled-1.1.safetensors` for stage 2 (its stage 1 ran at 16–18 s per step before refusing). Measured (match_4 A → B, 384×512, 25 fps, seed 0, no `--low-ram`): 25 frames in 335.7 s wall (Gemma 3.9 s, prompt 15.8 s, stage 1 twenty guided steps at 12.9–13.3 s, stage 2 three steps at 12–19 s, decode 7.8 s), peak RSS 9.8 GB; the same seed again 325.7 s, 14.3 GB, the mp4 byte-identical (one md5); 49 frames in 436.1 s (stage 1 at 17.6 s per step, stage 2 51 s), 13.0 GB. Result: both clips hold the start photo (gray MAD to A 21, to B 37 at 384×512) and switch to the finish photo at frame 14 of 25 and 18 of 49 (MAD to A 36, to B 16) with a 34-level adjacent step equal to the A–B gap; no in-between frame; endpoints re-encoded (MAD 21 / 16). Cause not established; the untested levers are the conditioning strengths (1.0 both ends), a prompt that names the change as motion, 97 frames, the q8 pack. Beside it, SD 1.5 inpainting (the line above) gives an intermediate mural at 15 s per 512×640 frame but no temporal coherence. Neither generative route is adoptable yet; the deterministic `hold` + effect design stands. Next experiments in the handover, one variable each. real-pair-VERIFIED for the numbers (match_4). [tier: Standard] [revert: n/a — measurement; the scratchpad is session-local]
- **2026-09-15 (late) — Owner picks on the TR14 page: 0 of 4 acceptable; the position hold reads as a fade; the next probe combines a motion-bearing field with a partial luma reveal.** File `benchmarks/runs/2026-09-15/tr14/tr14_picks.json`, verbatim: match_4 `dis` — "I want even more transformation, but if i could mix dis + maybe a bit luma looks promising ( obviousuly keep researching other stuff) , melt is too wobly . And even dis and luma i suspect work only becouse it is very close match "; match_5 `hold-dis` — "I want even more transformation, but if i could mix - hold-dis + maybe a bit luma looks promising ( obviousuly keep researching other stuff) , melt is too wobly . And even hold-dis and luma i suspect work only becouse it is very close match "; match_3 `roma` — "other approaches still just fade out - fade in of a person ( e.g hold-dis in this case)  , only roma somewhat resambles fluid upper motion  ( still with artifacts on person edges) , all other distorts person too much "; mismatch_7 none — "hold and hold-dis , nice attempt to transform clouds at least( no better then previous runs, too chaotic)  , everything else is either simple crossfades with additional montion or too noisy. also still can see hard edges  of start frame in transition". Owner, overall (verbatim): "hardly see any improbements since previous runs". Effects on the design (`TRANSITIONS.md §10.6`, sheet §Owner picks, plan TR14): the position hold is necessary (match_5's pick is the held field) but not the improvement the owner grades; `hold` alone is the fade RoMa was; `melt` is dropped; the next probe puts the `luma` reveal at partial strength over `dis` / `hold-dis` (a `reveal_strength` knob) and adds a stroke-flow motion along the new content's structure; match_3 confirms the RoMa field for a moved person (TR9); mismatch_7's "hard edges of start frame" confirms backlog T14 by the owner's eye. No build until a pick is acceptable. The design pass's yield stands as recorded (TR2d-as-written rejected with numbers; the residual-form floor; the mask-gate rule for displacement effects), and the owner's verdict on it is quoted, not softened. [tier: Quick] [revert: n/a — record]
- **2026-09-15 (late) — Owner direction after the TR14 picks: mismatched pairs are the main problem; the generative tier is the route, with the deterministic skeleton kept as the steering and the detail source; experiments and resources approved in blanket.** Owner (verbatim): "schedule everything relevant above for next session and to proceed with the plan , i am ok with any experiments , tests and additional resources as long as we have more options to test. While we want to imporve and make match pairs more interesting , the main problem remains with mismatches , the still more reseble simple crossfades with various cheap effects on top and not truthful continous transformations ( i guess this is where we will venture into generative area but i want it also to keep all good computational,  flow and other tech aspects and math  we have in project if this helps to keep as many details from both pictures and bridge them with unexpected cool flowing transition - this is just my rant, not some specific instructions for you , simply to give you piece of my mind, we still have .claude/claude-docs/transitions-research/impossible-0.1.0/impossible/RESEARCH-PLAN.md, see where we can push it even more". Effects: plan §Rank revised (2026-09-15 late): TR6-A the generative bridge on the skeleton (research E17: SD 1.5 SDEdit from the skeleton frames, warped noise along the skeleton's flow, frequency-split lift; then DiffMorpher and DreamMover as two-image morphers) → TR6-B LTX-2.3 levers in the background → TR10 depth camera move → TR9 semantics → TR14 second probe on the match pairs. The research plan's own gates stay (E14: ≤ 10 s per 480p frame and a fixed seed reproduces; E16: ≤ 6 min per pair; E17: at dreaminess 30 the subject's path is the skeleton's); its offline contract too (warmup + manifest + refuse-to-download before anything lands in the repo). Two LTX levers (motion prompt; end-frame strengths 0.8; 49 frames) were started in the background at the end of this session; results in the handover if they finished. Handover rewritten with the schedule. [tier: Quick] [revert: n/a — record and rank]
- **2026-09-15 (late) — LTX-2.3 levers: the endpoint conditioning strength, not the prompt, decides whether the model interpolates; at 0.8 the 49-frame clip is continuous.** Same setup as the line above (int4, dev transformer, CFG 3.0, 1.1 LoRA, match_4 A → B at 384×512, seed 0, 49 frames). Motion prompt alone ("time-lapse: a street artist paints over the graffiti on the utility box, the mosaic mural gradually appears tile by tile, static camera"): still a cut, now at frame 7 (adjacent step 38.6 levels), then a slow drift toward B (gray MAD to B 22.9 → 15.7 over 40 frames); 479.8 s. `--start-strength 0.8 --end-strength 0.8` with the default prompt: no cut — the distance to A rises 21.6 → 35.6 and to B falls 37.0 → 16.2 monotonically, largest adjacent step 4.23 levels; by the agent's eye the graffiti thins while the mosaic emerges under it, box and wall hold; 539.7 s wall beside another run, peak RSS 14.4 GB. Endpoints remain re-encoded (MAD 21 / 16 at 384×512): an adopted clip takes its byte-exact frames 0 and n−1 from the skeleton. Clips, strips, decode JSON, the run script and the decoder copied to `benchmarks/runs/2026-09-15/ltx/` (gitignored). Owner, same evening: Homebrew ffmpeg reinstalled (9.0.1 works; the PATH shim is retired), `HF_TOKEN` placed in `.claude/.env` (gitignored, never printed) for gated weights, "regarding q8 pack download, i am open for anything". Next levers, one at a time on the 0.8 base: strengths 0.6 / 0.9, 97 frames, a mismatched pair, the motion prompt on top, then the q8 pack (keyframe subset about 59 GB; RAM 36 GB against the port's 32 GB+ recommendation for int8, `--low-ram` as the fallback). real-pair-VERIFIED for the numbers (match_4); the look is the owner's verdict. [tier: Standard] [revert: n/a — measurement]
- **2026-09-15 (fourth session) — TR6-A measured: the generative bridge on the deterministic skeleton passes the research plan's E17 gate on all three pairs once the strength is ramped and the frames are filtered along the skeleton's flow; the tools are untouched.** Script `scripts/research/gen_bridge.py` (skeleton → SD 1.5 inpainting SDEdit on MPS fp16 → measure → page; the research artifact's `warp_noise_along_flow` and `frequency_split_blend` ported), pairs mismatch_1 (class B pan-zoom skeleton), mismatch_4 (same), match_4 (TR14 `hold-dis` skeleton, generator inside the changed mask), 30 frames, 512 px long edge, guidance 6, seed 0, frames 0 and n−1 the skeleton's byte for byte. Numbers (sheet `benchmarks/2026-09-15-generative.md §1`; page `benchmarks/runs/2026-09-15/gen/index.html`): mean adjacent step, skeleton → fresh noise → warped noise at strength 0.4 (levels): mismatch_1 3.36 → 11.88 → 9.19, mismatch_4 2.08 → 7.21 → 6.91, match_4 2.06 → 9.45 → 4.75; the last generated frame jumps 7–15 levels into B. With the strength ramped as 0.6 · sin(πu) and a filter along the skeleton's flow (each frame averaged with two neighbours each side warped into it, weights 1 2 3 2 1): 3.86 / 2.89 / 2.65 = 1.15× / 1.39× / 1.29× the skeleton, steps into B 3.1 / 2.6 / 2.9, content 11.6 / 7.7 / 7.9 levels from the skeleton; lifted to the native canvas (generator low frequencies under the skeleton's detail, σ 3 px) 4.58 / 3.40 / 3.68 against 4.00 / 2.69 / 3.18. Same seed byte-identical (5 frames, max abs diff 0). Generator 6.0–13.4 s per frame at 0.4 and 13.3–18.0 at 0.6 beside other GPU jobs (3.5–6.5 min per clip), peak RSS ≤ 0.8 GB. Two mechanism facts for the record: diffusers 0.40's inpaint pipeline treats a passed `latents=` as pure noise when strength < 1 (it skips the image + noise mix), so the warped noise goes in by patching the module's `randn_tensor`; noise warped at 1/8 resolution rounds subpixel motion away, so it is warped at pixel resolution and block-summed. By the agent's eye (the owner's picks decide): mismatch_1 reads as a transformation (the city stays a city while the sky changes), match_4 gets a different mural in every frame (a per-frame model has no memory), mismatch_4 at 512×288 gets blocky tiles in the sky and water. The TR10 depth skeleton under the bridge doubles the flicker (13.6 / 13.3 against 6.9 / 9.2). Next levers (handover): weaker LTX anchors, `retake` on the skeleton clip, an auto-regressive input, a 768 px canvas for wide pairs. real-pair-VERIFIED for every number; the look is UNVERIFIED until the owner's picks. Docs: `TRANSITIONS.md §10.7` (amended) and §6 rows, plan TR6 row. [tier: Deep] [revert: n/a — research script and pages; `git diff` empty on the four roster files; nothing in the tools]
- **2026-09-15 (fourth session) — TR6-B and TR6-A2: LTX-2.3 keyframe interpolation cuts on both mismatched pairs even at endpoint strength 0.8; with five skeleton frames anchored it is continuous but copies the skeleton's crossfade; the two published two-image morphers fail the E16 gate.** LTX (int4 MLX port, dev transformer + CFG 3.0 + 1.1 LoRA, 49 frames, seed 0; clips `benchmarks/runs/2026-09-15/ltx/`): mismatch_1 at 0.8 → a cut at frame 28 (38 levels) after the clouds drift for 27 frames, 1849 s beside two GPU jobs, endpoints 5.3 / 5.3; mismatch_4 at 0.8 → a cut at frame 31 (21 levels) after the sun drifts toward B's composition, 1961 s, endpoints 7.4 / 10.7; match_4 at 0.6 → continuous like 0.8 (max step 4.8, monotone), 736 s, endpoints 22.4 / 16.8. `generate --two-stage` with the skeleton's frames 0 / 12 / 24 / 36 / 48 anchored at 0.8 / 0.6 / 0.6 / 0.6 / 0.8 on mismatch_1 → continuous (mean step 2.28, max 3.87, endpoints 4.4 / 8.4, 1107 s, RSS 13.8 GB) and 11.3 levels from the 49-frame skeleton clip: it reproduces the skeleton's double exposure at frames 24–32 rather than resolving it. The lever script was stopped after match_4 at 0.6 (strength 0.9, 97 frames and the q8 pack not run) in favour of the anchored mode and `retake` of the skeleton clip's middle latents; the `retake` run and the mismatch_4 anchored run were in flight at the end of the session (outputs in the session scratchpad `ltx_out/`; the handover names them). Morphers (sub-agent tracks, reports `benchmarks/runs/2026-09-15/morphers/*_REPORT.md`): DreamMover (SD 1.5) runs on MPS after five patches (the cupy softsplat kernel in PyTorch, chunked attention for xformers, a device helper, a vectorised feature flow, batch 34): 18.2 / 12.0 min (mismatch_1, two runs) and 8.8 min (mismatch_4) at 512 px, memory footprint 18–20 GB, deterministic to 1 level (no seed exists), endpoints re-drawn (6.6 / 4.5 and 3.7 / 4.6 levels from the photos), a seam at frame 16 where its two latents swap; NO licence file (commit `bdfaa3a`) — nothing can be vendored; its sun-to-sun clip is the most morph-like result of the session (the sun travels with its reflection). DiffMorpher (SD 2.1-base, S-Lab 1.0 non-commercial): installs and runs on MPS, but `stabilityai/stable-diffusion-2-1-base` returns 404 to the owner's token (the licence is not accepted on that model page; `whoami` OK); a random-weight UNet of the exact shape measures 0.502 s per fp32 step and 2.43 s per LoRA step → 42 min per pair at the README's recipe, 7.5 min minimal, fp16 2.5× slower than fp32. Nothing in the repo; every weight and venv stays in the session scratchpads. real-pair-VERIFIED for the numbers; the look is the owner's verdict. [tier: Standard] [revert: n/a — measurement]
- **2026-09-15 (fourth session) — TR10 first measurement: a depth-modulated camera move renders without tearing but is still a crossfade with parallax; as the bridge's skeleton it doubles the flicker.** Script `scripts/research/depth_dolly.py` (Depth Anything V2 Small, Apache-2.0, transformers 5.17 on MPS in the SD scratch venv; the class B pan-zoom field × (0.5 + disparity) with the disparity as the splat importance; `--export` hands the field to `gen_bridge.py` as a skeleton), mismatch_1 / mismatch_4, 2 s clips at the native canvas, page `benchmarks/runs/2026-09-15/depth/index.html`, sheet `§4`: depth 0.3–2.7 s per image; mid-frame holes ≤ 0.9 % of the canvas at 10 % and 25 % zoom (the T13 number); mean step 1.61 / 1.05 (10 %) and 2.24 / 1.38 (25 %) against the uniform pan-zoom 1.79 / 1.15 and 2.60 / 1.61; warping error equal or lower; 14–34 s per 60 frames. The layered mesh with inpainted disocclusions (research E11 as written) is not built. Plan TR10 row updated. real-pair-VERIFIED for the numbers. [tier: Standard] [revert: n/a — research script]
- **2026-09-15 (fourth session, late) — LTX `retake` of the skeleton clip's middle re-encodes the skeleton's crossfade; a strong skeleton guide is followed, not resolved.** `retake --video mismatch_1_skeleton49.mp4 --start 2 --end 5` (latent frames 2–4 = pixel frames 9–32 of the tool's 49-frame class B clip at 384×512, 30 steps, CFG 3.0, seed 0): continuous and monotone (mean adjacent step 1.97, max 2.73), endpoints 4.4 / 4.0 levels from the photos, 1505 s beside the SD batch, RSS 15.0 GB; the regenerated frames are 4.6 levels from the skeleton clip against 2.4 for the kept frames (the codec's own difference), so the model reproduced the double exposure it was given. With the five-anchor `generate` run of the line above (11.3 levels from the skeleton, the double exposure copied) this closes the "video model steered by the skeleton" question for guides at strength ≥ 0.6: the next lever is a weaker guide (middle anchors at 0.3) or the per-frame SD bridge with an auto-regressive input. Clip and decode JSON in `benchmarks/runs/2026-09-15/ltx/`; sheet §3; `TRANSITIONS.md §10.7` fact 4. real-pair-VERIFIED for the numbers. [tier: Quick] [revert: n/a — measurement]
