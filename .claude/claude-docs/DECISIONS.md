# Reveal — Decisions Log

One line per meaningful change: what was decided, files touched, numbers measured, verification
tier (harness-tested / real-pair-VERIFIED / UNVERIFIED). **Append-only, absolute-dated.**
Supersede a past line with a newer dated line; never edit or delete old ones. Owner orders are
quoted VERBATIM ("Owner order (verbatim intent): …"), never paraphrased. Product decisions taken
before 2026-09-13 live in `HANDOFF.md §6` (27 rows; the file moved to `.claude/claude-docs/` on
2026-09-13) and are cited by number, not copied here.

Compaction (a move is not an edit): at every era close, and whenever the hot section passes ~100 KB
(≈25k tokens — it must fit one read; the boot hook warns), move the closed era BYTE-VERBATIM to
`DECISIONS_ARCHIVE.md` under a dated `## Moved <date>` divider, record the md5 + line/byte count of
the moved block here in a Compactions table, append a 10–30 line era digest in its place (every
caveat and number preserved), and re-seat the append marker under the heading. Proof of purity =
the removed line count equals the moved block; no dated line changed.

## Compactions
| Round | Date | Moved | md5 | Lines / bytes |
|---|---|---|---|---|

## Traps & gotchas (durable — promoted from `HANDOFF.md`, each naming its origin)
- `findTransformECC` maps TEMPLATE(A) coords into INPUT(B) coords (applied with `WARP_INVERSE_MAP`): seed with `inv(H)`, invert the result back. Cost a bug in the founding build (2026-07-10, `HANDOFF.md §3.2.6`).
| 1 | 2026-09-26 | era 2026-09-13 → 2026-09-23 (lines 42–78 at e47de55) → DECISIONS_ARCHIVE.md | 70a09c0ee2da631f62cee0f701c0ea9d | 37 / 69832 |
- `scale_h` transport shipped inverted once; harness check 19 pins it with ground truth (2026-07-10, `§3.3`).
- rawpy applies the camera flip itself — re-applying EXIF orientation to RAW output double-rotates (2026-07-10, `§3.1`).
- Grayscale change detection misses equal-luminance repaints; per-channel max over normalized channels (harness 21 failure, 2026-07-10, decision 4).
- An unsigned edge detector, or a window spanning two parallel structures, fabricates 5–12 px of fake wobble; judge wobble as residual from a fitted quadratic with signed single-transition tracking (field defect #1, 2026-07-12, `§5a`).
- Inlier counts are not comparable across matchers (SIFT 458 → SSIM 0.566; learned 408 → 0.429); arbitrate on peripheral SSIM (2026-07-13, decision 23).
- Optional code that is never executed is a liability: the learned path shipped unrun for three releases behind a `try/except` returning `None` (2026-07-13, `§5b`).
- kornia's LoFTR derives stride and x-scale from the HEIGHT alone; non-multiple-of-8 inputs mis-scale keypoints (2026-07-13, `§5b`) — moot since LoFTR is out, kept as a class: check a model's input-shape contract before feeding it resized images.
- A float→uint8 `astype` TRUNCATES: every composited frame lands half a level low, and against a byte-exact pinned endpoint that is a visible step (0.8 levels at the last frame of a clean dissolve, hidden-cut detector 1.96 instead of 0.02). Round (`to_u8`) at every cast; the transitions harness pins it (checks 11, 20, 24; 2026-09-13, `transitions.py`).
- A review surface must say what varies. Eight time-sampled frames of one transition, shown as a strip with no labels, were read by the owner as eight variants to choose from (2026-09-13, `benchmarks/runs/2026-09-13/index.html`). Label each tile with frame and time, put the variants' differences on the page, and give the reviewer a pick control that produces a file the sheet can ingest.

## Recent
<!-- append below this line -->
- **2026-07-10 — v1.0.0 founding build** (backfilled from `HANDOFF.md`). Classical pipeline (SIFT/ORB + MAGSAC++ + ECC), optional learned fallback (LoFTR then), auto-gated residual flow, stdlib HTTP server on 127.0.0.1, embedded page, wipe/fade video via piped frames; 51 harness assertions. Decisions 1–10 in `HANDOFF.md §6`. harness-tested (Linux sandbox).
- **2026-07-10 — v1.1.0 matching modes** (backfilled). `MODES = {reshot, loose}` as a CFG overlay; 4-DOF similarity fallback and always-on learned matcher in loose; residual hard-off in loose. Decisions 11–13. harness-tested.
- **2026-07-10..13 — v1.2.0 → v1.3.0 field defect #1: wavy subject edges** (backfilled; exact day not recorded). Root-cause chain reproduced in harness 63–65 before fixing; three designs rejected empirically (feathered kill zone, distance-ramp attenuation, per-subject rigidification); final: DIS flow as samples only → robust 20-parameter cubic field, whole-field rejection over 6 work-px. Wobble 8.5 px → 0.09–0.12 px on the reproduction. Decisions 14–20. harness-tested; real-pair-VERIFIED on the graffiti-box pair (box edge 0.54 px RMS vs camera original 0.62).
- **2026-07-13 — v1.3.1 confidence rescored on geometry only** (backfilled). Pixel-similarity terms (absolute SSIM, peak sharpness) removed as scene-dependent in opposite directions; local-max boolean kept as a ×0.6 penalty. Decision 21. real-pair-VERIFIED (first real pair: sift, 458 inliers, 0.74 px rmse → 95).
- **2026-07-13 — v1.4.0 learned matcher first executed and replaced** (backfilled). LoFTR out (plain-HTTP weights, input-shape bug, untestable); DISK + LightGlue in; cross-matcher arbitration on peripheral SSIM; latent `max([])` crash fixed. Decisions 22–23. harness-tested (77–79).
- **2026-07-13 — v1.5.0 learned matcher made genuinely offline** (backfilled). `TORCH_HOME` pinned to `models/`; manifest-gated runtime that refuses to download; warmup runs a real forward pass and records sha256s. Decisions 24–27; harness 80–82 (all sockets patched to raise). Footprint 2 files / 52 MB. harness-tested.
- **2026-07-21 — Repo initialised on GitHub** (`sukhov-work/re-veal`, commits `e51b4e5` init, `344e4c4` cleanup): source, harness, docs and the 52 MB of weights committed as-is. No behaviour change.
### Era digest 2026-09-13 → 2026-09-23 (37 lines moved to DECISIONS_ARCHIVE.md on 2026-09-26; md5 70a09c0ee2da631f62cee0f701c0ea9d)
- 2026-09-13: The operating environment was bootstrapped (bootstrap-project v2, Scaffold mode; Python 3.13 venv, laptop-only + GitHub publish remote, Serena on the JetBrains backend, Codex present, fan-out 3): `AGENTS.md` + `CLAUDE.md` import, `.claude/settings.json` (`models/**` denied), hooks `session-start.sh` (verified, 0.7 s) and `pre-compact.sh` + `.codex/hooks.json` (both UNVERIFIED), six conventions files, the `/reveal` skill with tracked backlog T1–T6, memory under `.serena/memories/` (core 3.2 KB, 8 files), this log (7 backfilled lines, 8 promoted traps), `EXPLORATION_PLAN.md` DRAFT (H1–H3, E1–E3), `scripts/clean.sh`; 3 tracked `.DS_Store` untracked. Skipped: Phase F delivery artefacts, SessionEnd auto-ship, lint gate (T6). Gate baseline: harness 82/82 in 62.7 s, `check` all OK in 18.5 s. Owner order (verbatim in DECISIONS_ARCHIVE.md, Moved 2026-09-26). harness-tested.
- 2026-09-13: Slice TR1 shipped `transitions.py` (1,076 lines, nine sections, 7 presets, class A homography+DIS / class B saliency-or-MLS, 1/4-res splat morph, Lab color path, 480-px quality basket, imageio-ffmpeg, CLI `pair` + `check`) and `transitions_harness.py` (36 checks, 5.3 s), ported from the research artifact Impossible v0.1.0 (its harness 34/34 in 4.5 s; E1 bench 720p 0.086 · 1080p 0.226 · 2160p 0.728 s/frame; its `REVEAL-BASELINE.sha256` matched all nine roster files). Not ported: package layout and separate venv, PyAV (T8), RoMa-on-MPS and runtime download (T11), generative stubs, clips/sequence (TR4). Defect fixed: float→uint8 truncation. Synthetic 1200×900 pair: class A, 214 inliers, median displacement 0.19 px, morph 3.1 s / wipe 1.6 s for 30 frames, `edge_ratio` 0.44 (1.83 before the fix). Roster: harness 82/82 (71.5 s, concurrent bench), `reveal.py` byte-identical. Backlog T8–T11 opened. Real-pair claims UNVERIFIED at that point. Owner order (verbatim in DECISIONS_ARCHIVE.md, Moved 2026-09-26).
- 2026-09-13: Two agent rulings, both later confirmed or superseded below: the dense-flow fence (`HANDOFF.md` decisions 16, 18, 20) covers Reveal's alignment output only, so the transitions morph field does not cross it, with the §5a content-chasing risk left for TR2 to measure; transitions stay CPU-only and weight-free until a warmup exists (MPS out per decision 9; research E6 torch/MPS splat port not pursued; any learned matcher only behind Reveal's v1.5.0 shape: setup-time download + real forward pass + sha256 manifest + refuse-to-download; harness check 3 pins it).
- 2026-09-13 (evening): Owner answers (verbatim in DECISIONS_ARCHIVE.md, Moved 2026-09-26): "leave it as `transitions` and record as official name"; "dense-flow decision - ok"; speed up all stages "without compromising quality"; gitignored fixtures inside the project; push "direct to `origin main` all the time"; the /no-slop rule. Effects: harness checks 37 (persisted names) and 38 (research codename absent), Gate 1b 38 checks; the fence scoping became an owner decision in `HANDOFF.md §11`. Device rule (supersedes the morning line in one respect): TR5 and TR6 choose their device per slice by a measured run against a CPU run, field difference stated in px; decision 9 stands for Reveal; nothing downloads at runtime. Morph frame profile (M3 Pro, 1080p / 4K): total 0.160 / 0.689 s; color path 0.055 / 0.243; hole fill + mix + cast 0.049 / 0.213; two forward splats 0.040 / 0.159; two backward warps 0.017 / 0.071; correspondence once 0.36 / 1.51. First TR7 step (`color_cache`, in-place hole fill): frames byte-identical by sha256 over all presets, 1080p frame 0.163 → 0.151 s; TR7 targets 0.10 s at 1080p, 0.45 s at 4K.
- 2026-09-13 (evening): Plan ranked TR7, TR2, TR5, TR6, then TR3 and TR4, then H1/H3/H2 and E1/E2. Fixtures at `fixtures/` (`/fixtures/*` ignored, `!/fixtures/MANIFEST.md` tracked); T4 then blocked only on the first pair. No lint gate; T6 DEFERRED. Phase 4 ends with a commit on `main` and `git push origin main`, never with a red gate. `HANDOFF.md` and `TRANSITIONS.md` moved under `.claude/claude-docs/`; `HANDOFF.md §11` Amendments added; T7 CLOSED; prose gate `.claude/rules/prose.md` added.
- 2026-09-13 (late): Commit `39d2c67` unintentionally carried `.claude/skills/no-slop/SKILL.md` and `SKILL-DNA.md` (the Skill tool copied the global skill at 21:12); history not rewritten; T12 opened (closed 2026-09-14: the vendored copy stays).
- 2026-09-13 (late): First real fixtures: 5 `match` + 5 `mismatch` pairs (18 JPEG + 2 HEIC; the HEIC files 6048×8064 with ICC, 1.9 s decode each; EXIF orientation 6 on `match_5_S` handled). Reveal on `match_1` (first real HEIC): reshot passes, sift, 368 inliers, 0.74 px, confidence 95. Transitions `match_1` / `match_2` (canvas ≤ 1920 px): class A, 492 / 899 inliers, median displacement 63.7 / 140.8 px, morph `edge_ratio` 0.23 / 0.12. TR2 sheet via new `scripts/bench_transitions.py --reveal`: 60 runs; Reveal reshot passes all five match pairs (sift; inliers 121–1059; rmse 0.67–1.01 px; confidence 90–97) and refuses all five mismatches; transitions class A on matched, class B on mismatched (6–36 inliers); `edge_ratio` ≤ 0.55 on smooth presets, 1.23–1.99 on snap-morph; endpoints 0 / 0; morph 1 s renders 4.7–7.9 s. Roster pair `match_4`: 481 inliers, 0.79 px here vs 458 / 0.74 in the sandbox (environment, not code). T4 CLOSED. real-pair-VERIFIED.
- 2026-09-13 (night): Owner added mismatch_6 (day → night, 1146×1528 vs 3056×4064; Reveal refuses; class B with 0 inliers) and mismatch_7 (same skyline, different skies; Reveal strict PASSES: sift, 78 inliers, 0.89 px, confidence 92; transitions class A, 70 inliers, mean certainty 0.053 against 0.33–0.81 on match pairs; the morph tears the clouds, field defect #1's mechanism). The sweep became incremental (`run_at`, `--only`). Sheet 12 pairs, 72 runs, routing 11 of 12 as labelled, smooth-preset `edge_ratio` ≤ 0.79. TR2b (certainty floor) proposed. Owner's first review (verbatim in DECISIONS_ARCHIVE.md, Moved 2026-09-26): the eight tiles were read as variants; match_1/4/5 "promising", match_2 slides sideways, match_3 dissolves, mismatches "naive cross-dissolves". The page was rebuilt (labelled frames, inline clips, `picks.json`, `--render-only --picks`). Diagnosis: match_2's person is a 141 px residual; match_3's poses share no flow. Rank: TR5 → TR9 → TR6 → TR7 → TR2b (folded into TR5) → TR3/TR4 → H/E. E2 gate (≥ 6 of 10 usable) not met.
- 2026-09-14: Owner answers (verbatim in DECISIONS_ARCHIVE.md, Moved 2026-09-26): picks file `picks_1-14-10-2026.json`; the finish ratio wins the canvas (letterbox and common-area modes later, TR13); mismatch_7 is a rough match "never intended to be aligned"; README waits for satisfying results; T8 PyAV approved for TR4; "YES, this is personal project, fetch anything you need" (each weight still behind warmup + manifest + refuse-to-download, license recorded); keep vendored no-slop (T12 CLOSED); T9 CLOSED. Closest-to-intent picks: match_1 flow-dissolve 1 s, match_4 morph 3 s, match_5 morph 3 s; the rest none. Correction the same day, owner: "they simply resable what i am looking for but very far from perfect" — so 0 of 12 usable as-is, 3 closest-to-intent picks; lesson: a pick control needs a separate "acceptable as-is" answer. New defects: class B border walks (T13 → TR2c); texture from one side on match_4 and box shift on match_5 (TR2d).
- 2026-09-14: Canvas policy `finish` (default) — the finish image's aspect, largest rectangle both cover without upscaling (`w = min(wA, hA·r, wB)`, `h = w/r`, even); `common` keeps the old rule; CLI `--canvas finish|common`. Four of twelve canvases changed (match_5 1518×1920 → 1438×1920; mismatch_2 730×800 → 602×800; mismatch_4 1920×1236 → 1920×1092; mismatch_6 1146×1528 → 1146×1524). Harness J, checks 39–40 (check 40 rewritten the same hour after a collapsed conditional); Gate 1b 40/40. [revert: `--canvas common`]
- 2026-09-14 (evening): TR2c shipped; T13 CLOSED. Before the fix the mid frame showed one photo inside the other (mismatch_1 18.6 % of the canvas without B, mismatch_3 36 %, mismatch_4 27 % without A). `panzoom_field`: zoom `classb_zoom` = 0.10 about the salient center plus a capped pan (A zooms 1 → 1.1, B 1.1 → 1); method `saliency-panzoom`, `pan_fraction` in `report.json`; mismatch_3 fractions 0.06 / 0.06, mismatch_4 suns 64 px apart at mid. Harness K, checks 41–43 (old shape 12 % uncovered and a 56-level step; min coverage 0.81 at t = 0.25..1, far target 2 %, near 100 %; blob within 5 px, coverage ≥ 0.86, 4-level step); the pan-clip mutation turned 42 and 43 red (coverage 0.00, 56-level step). Class A frames byte-identical (sha256, seven presets). Sweep: 60 runs exit 0, endpoints 0 / 0; class B median displacement 46–122 px (was 232–889); morph 1 s `edge_ratio` 0.13–0.66 (mismatch_6 0.66, was 0.52; its flow-dissolve 1.04); match_5 now 1438×1920, 89 inliers, 84.6 px. Gates: harness 82/82, transitions harness 43/43 (7.2 s), both `check`s OK. The 0.10 zoom has no source beyond the agent's eye. [revert: commit; `classb_zoom` 0 = crossfade]
- 2026-09-14 (evening): RoMa in a scratch venv (romatch 0.1.2, MIT; torch 2.14.0, torchvision 0.29.0, timm 1.0.29): weights `roma_outdoor.pth` 445,647,516 bytes (sha256 c7a45c80d41ad788a63c641d1b686d7cb3f297f40097c6f4e75039889e5cc8ba) and `dinov2_vitl14_pretrain.pth` 1,217,586,395 bytes (sha256 d5383ea8f4877b2472eb973e0fd72d557c7da5d3611bd527ceeb1d7162cbf428, Apache-2.0); inputs resized to 560×560 coarse and 864×864 field, aspect ignored; autocast disabled off CUDA ("mps is not supported"); symmetric warp (1, H, 2W, 4); the VGG19 encoder has no third weight file. match_4 at 1536×1920, 12 threads: 40.6–44.8 s per match vs 0.74 s homography+DIS; certainty mean 0.553, 62 % above 0.5; median displacement 9.9 px (DIS 15.1); median difference to DIS 0.57 px, 0.20 px where both confident (52 %). Then, owner (verbatim): "timings below 1-2  min per pair  is not that much issue for now , lets achieve best quality and then see if and how to optimize". match_2 (896×1920) / match_3 / match_5 (1438×1920): 55.4 / 37.2 / 34.8 s, load 8.7 s; median field difference 0.5 / 9.2 / 22.1 px; share over 5 px 17 / 58 / 63 %; certainty 0.43 / 0.16 / 0.27 (DIS 0.81 / 0.41 / 0.32); warping error DIS → RoMa 0.0068 → 0.0068, 0.0038 → 0.0035, 0.0211 → 0.0162; `edge_ratio` 0.16 → 0.19, 0.13 → 0.09, 0.24 → 0.21; renders 3.9–6.2 s. Agent score 2 wins, 2 ties of 4; the "certainty holds regions in place" reading was withdrawn on 2026-09-15. Research scripts `compare_fields.py`, `roma_probe.py` tracked under `scripts/research/`. TR5 integration plan then: warmup + manifest + refuse-to-download, lazy torch import with check 3 rewritten, `--dense roma|dis`, CPU only until a measured MPS equivalence, weights proposed at `models/transitions/`; TR7 targets do not apply to the dense matcher.
- 2026-09-15: Owner review of the 2026-09-14 run (verbatim in DECISIONS_ARCHIVE.md, Moved 2026-09-26): no mismatch acceptable, improvements "marginal", asked for RoMa as full video clips. mismatch_7's morph 1 s mp4 had the same md5 (df5e516d4f0fcaaec215833e460af668) in both runs, so its seen improvement was not code. Corrected mechanism: where RoMa certainty < 0.3 (84 % of match_1, 78 % of match_3, 66 % of match_5, 96 % of mismatch_7) its displacement departs from global motion by 9.3 / 15.7 / 24.3 / 350 px median vs DIS 11.9 / 32.2 / 46.4 / 243 px; certainty only weights splat collisions; mismatch_7 certainty 0.03, forward-backward error 227.7 px. RoMa on match_1 / match_4 / mismatch_7: 46.4 / 46.4 / 46.1 s, certainty 0.13 / 0.55 / 0.03. 24 clips; warping error DIS → RoMa on morph 1 s: match_1 0.0064 → 0.0070, match_2 0.0109 → 0.0121, match_3 0.0038 → 0.0037, match_4 0.0102 → 0.0080, match_5 0.0195 → 0.0176, mismatch_7 0.0120 → 0.0194; flow-dissolve `edge_ratio` rises with RoMa on every pair (0.23 → 0.64 on match_1), cause not established.
- 2026-09-15: Owner verdicts on the RoMa clips (`roma_picks.json`, verbatim in DECISIONS_ARCHIVE.md, Moved 2026-09-26): match_1 same, match_2 neither, match_3 roma better, match_4 / match_5 / mismatch_7 dis better; RoMa fails the TR5 ≥ 5 of 7 rule at 1 of 6 and is not integrated as a drop-in field. Owner correction (verbatim): "i liked how the box itself holds position" — the box holding position is a win; the changed content fading is the complaint. TR14's brief: the changed object keeps its place AND its content transforms. Lesson: the basket screens for defects; the owner's clip verdict is the quality metric. Timing rule (owner): 3–5 min per pair acceptable for experiments, "no hard upper limit ( reasonably still)".
- 2026-09-15: TR14 design (`TRANSITIONS.md §10`, probe `scripts/research/tr14_variants.py`, 66 clips, endpoints 0 / 0). Changed-region mask share: match_4 0.276, match_5 0.49, match_3 0.189, mismatch_7 0.432; photo box-edge straightness 2.54 / 3.89 px on match_4. TR2d as written rejected: match_5's boxes move 2.9 px against 70 px camera motion, mismatch_7's sky 0.4 px against 330 px, while warping error falls 0.0176 → 0.0119 (the basket cannot see it). `hold` = camera + (field − camera) × certainty keeps objects in place (5.2 / 72.8 / 4.2 / 330 px), edge 2.54 px vs `dis` 6.3, mismatch_7 warping error 0.0081 vs `dis` 0.012 and `roma` 0.0194. `hold-dis`: straightness 4.4 px, `edge_ratio` 0.136 at 3 s vs 2.54 and 0.027 (8 px feather). Effects `luma`, `edge-grow`, `melt` (`melt` 5.31 → `melt-soft` 2.42 px past the 24 px dilation). `hold` on mismatch_7 exposes the start frame's border: backlog T14. Owner picks (`tr14_picks.json`, verbatim in DECISIONS_ARCHIVE.md, Moved 2026-09-26): 0 of 4 acceptable; match_4 `dis`, match_5 `hold-dis`, match_3 `roma`, mismatch_7 none; overall "hardly see any improbements since previous runs"; `melt` dropped; next probe `luma` at partial strength (`reveal_strength`) plus stroke-flow; no build until a pick is acceptable.
- 2026-09-15: TR6 research (primary sources): Wan 2.1 FLF2V-14B 82 GB (out on 36 GB); DiffMorpher S-Lab 1.0 non-commercial; Framer academic-only; DreamMover no license file; RIFE MIT and FILM Apache-2.0 without Apple Silicon evidence. SD 1.5 inpainting (MPS fp16, 2.0 GB, creativeml-openrail-m) on match_4 at 512×640, strength 0.5, 10 of 20 steps: 1.43–1.62 s per step, 14.3–16.2 s per image, load 49.8 s, RSS 2.41 GB, same seed byte-identical, another seed 13.0 levels; no temporal coherence (30 frames ≈ 7.5 min). LTX-2.3 via `dgrauet/ltx-2-mlx` (int4 pack 59.7 GB, keyframe subset 29.5 GB + distilled transformer 11.3 GB + Gemma-3-12B 4-bit 8.1 GB): three setup obstacles: broken Homebrew ffmpeg (libx265 dylib missing; PATH shim to imageio-ffmpeg 7.1), `keyframe` refuses the distilled model (needs `--dev-transformer` + `--cfg-scale 3.0` + distilled LoRA 1.1), `--low-ram` stage 2 needs the distilled transformer (its stage 1 ran 16–18 s per step). match_4 384×512 seed 0: 25 frames 335.7 s (Gemma 3.9 s, prompt 15.8 s, stage 1 twenty steps at 12.9–13.3 s, stage 2 three steps at 12–19 s, decode 7.8 s), RSS 9.8 GB; rerun 325.7 s, 14.3 GB, byte-identical mp4; 49 frames 436.1 s (stage 1 17.6 s per step, stage 2 51 s), 13.0 GB; both clips cut (frame 14 of 25, 18 of 49; gray MAD to A 21 / to B 37 before, 36 / 16 after; 34-level step). Strength lever: motion prompt alone still cuts (frame 7, 38.6 levels, then gray MAD to B 22.9 → 15.7 over 40 frames, 479.8 s); `--start-strength 0.8 --end-strength 0.8` is continuous (A distance 21.6 → 35.6, B 37.0 → 16.2, max step 4.23, 539.7 s, 14.4 GB); endpoints re-encoded (MAD 21 / 16). Owner the same evening: Homebrew ffmpeg 9.0.1 reinstalled, `HF_TOKEN` in `.claude/.env`, "regarding q8 pack download, i am open for anything"; q8 keyframe subset about 59 GB against 36 GB RAM (port recommends 32 GB+ for int8, `--low-ram` fallback); next levers 0.6 / 0.9, 97 frames, a mismatched pair.
- 2026-09-15 (late): Owner direction (verbatim in DECISIONS_ARCHIVE.md, Moved 2026-09-26): mismatched pairs are the main problem; generative route with the deterministic skeleton kept; blanket approval for experiments and resources. Rank: TR6-A generative bridge → TR6-B LTX levers → TR10 depth move → TR9 → TR14 second probe. Research gates kept: E14 ≤ 10 s per 480p frame + seed reproduces; E16 ≤ 6 min per pair; E17 subject path follows the skeleton at dreaminess 30.
- 2026-09-15 (fourth session): TR6-A (`scripts/research/gen_bridge.py`, SD 1.5 SDEdit on MPS fp16, 30 frames, 512 px, guidance 6, seed 0) on mismatch_1 / mismatch_4 / match_4: mean step skeleton → fresh noise → warped noise at 0.4: 3.36 → 11.88 → 9.19, 2.08 → 7.21 → 6.91, 2.06 → 9.45 → 4.75; last frame jumps 7–15 levels. Ramped 0.6 · sin(πu) + flow filter (weights 1 2 3 2 1): 3.86 / 2.89 / 2.65 (1.15× / 1.39× / 1.29×), steps into B 3.1 / 2.6 / 2.9, 11.6 / 7.7 / 7.9 levels from skeleton; lifted (σ 3 px) 4.58 / 3.40 / 3.68 vs 4.00 / 2.69 / 3.18. Same seed byte-identical (5 frames, max abs diff 0). Generator 6.0–13.4 s per frame at 0.4, 13.3–18.0 at 0.6 (3.5–6.5 min per clip), RSS ≤ 0.8 GB. Traps: diffusers 0.40 inpaint ignores passed `latents=` as noise when strength < 1 (patch `randn_tensor`); warp noise at pixel resolution and block-sum. TR10 depth skeleton doubles flicker (13.6 / 13.3 vs 6.9 / 9.2). Next levers then: weaker LTX anchors, `retake`, an auto-regressive input, a 768 px canvas for wide pairs.
- 2026-09-15 (fourth session): TR6-B LTX at 0.8 still cuts on mismatches: mismatch_1 frame 28 (38 levels, 1849 s, endpoints 5.3 / 5.3); mismatch_4 frame 31 (21 levels, 1961 s, 7.4 / 10.7); match_4 at 0.6 continuous (max step 4.8, 736 s, 22.4 / 16.8). Five anchors 0.8 / 0.6 / 0.6 / 0.6 / 0.8 on mismatch_1: continuous (mean 2.28, max 3.87, endpoints 4.4 / 8.4, 1107 s, 13.8 GB), 11.3 levels from the skeleton, copies its double exposure; strength 0.9, 97 frames and q8 not run. Morphers: DreamMover (five MPS patches) 18.2 / 12.0 min (mismatch_1) and 8.8 min (mismatch_4), 18–20 GB, deterministic to 1 level, endpoints redrawn (6.6 / 4.5, 3.7 / 4.6), seam at frame 16, no licence (commit `bdfaa3a`), most morph-like clip; DiffMorpher: SD 2.1-base returns 404 to the owner's token, random-weight timing 0.502 s per fp32 step, 2.43 s per LoRA step → 42 min per pair (7.5 min minimal), fp16 2.5× slower. Both fail E16.
- 2026-09-15 (fourth session): TR10 (`scripts/research/depth_dolly.py`, Depth Anything V2 Small, transformers 5.17 on MPS) on mismatch_1 / mismatch_4, 2 s clips: depth 0.3–2.7 s per image; holes ≤ 0.9 % at 10 % and 25 % zoom; mean step 1.61 / 1.05 (10 %) and 2.24 / 1.38 (25 %) vs uniform 1.79 / 1.15 and 2.60 / 1.61; warping error equal or lower; 14–34 s per 60 frames; field = class B pan-zoom × (0.5 + disparity), disparity as splat importance; layered mesh (E11) not built. LTX `retake` of frames 9–32: continuous (mean 1.97, max 2.73, endpoints 4.4 / 4.0, 1505 s, 15.0 GB), 4.6 levels from the skeleton vs 2.4 codec baseline, reproduces the double exposure. 2026-09-16: five anchors on mismatch_4 (rendered 512×256, not 288): mean 1.32, max 3.41, endpoints 7.6 / 12.7 (A–B 32.6), B distance 35 → 37.5 before falling, 11.7 levels from skeleton, 512 s, 14.4 GB, two suns at frames 24–32. Guides at strength ≥ 0.6 are followed, not resolved.
- 2026-09-23: Owner verdicts (verbatim in DECISIONS_ARCHIVE.md, Moved 2026-09-26): generative bridge rejected (`gen_bridge_picks.json`, all five `none`, "horrible neural slop"); the TR10 depth move liked, especially z25, wanted as an option; LTX clips rejected ("a cut or dumb fade"); "yes to model, and run it on matched pairs too". TR6-A, TR6-A2 and TR6-B parked. Depth Anything V2 Small: 24.8 M parameters / 99 MB fp32, CPU 0.34 s per 1024-px image (12 threads, resized to 518), MPS 0.08–0.09 s warm; a weight-free ramp correlates 0.62 / 0.82 (mismatch_1) and 0.93 / 0.83 (mismatch_4) with model disparity. Trap: macOS purged the 2026-09-15 scratchpads (46 GB LTX pack, SD and DreamMover weights, venvs) after three days; `benchmarks/runs/2026-09-15/` survived. The E17 gate measured flicker, not hallucinations.
- 2026-09-23: TR10 built: `transitions.py pair --camera flat|ramp|model --zoom` (zoom clamped to [0.02, 0.5], default 0.10); field scaled by `1 − g/2 + g·d` (`depth_gain` 1), importance `0.2 + 0.8·d`. `model` = `depth-anything/Depth-Anything-V2-Small-hf` (Apache-2.0, 24,785,089 parameters, 99,173,660-byte safetensors) via `transformers` 5.17.0 on the CPU, cv2 preprocessing (≤ 1024 px input, short side 518, multiple of 14, bicubic, ImageNet statistics, re-checked by warmup against `preprocessor_config.json`), output clipped to its 2nd–98th percentile; method `saliency-panzoom+ramp` / `+model`, `report.json` gains `camera`, `zoom`, `disparity_mean`. `transitions.py warmup` is the only downloader (`HF_HOME` → `models/depth/`; forward pass floor 0.866 vs sky 0.0; `models/DEPTH_MANIFEST.json` 5 files, 99,175,616 bytes, 4 links); refuse without manifest; torch/transformers imported lazily in four depth functions; `transformers==5.17.0` in `requirements-learned.txt`; backlog T15 (stale `pip` shim). Transitions harness progression in this era: 36 → 38 → 40 → 43 → 51; here 43 → 51 (section L, 44–51; ramp 127.4 vs 52.3 px, 2.44× vs 1.03×; gain-3 mutation 2.2 % holes; cold load 0.9 s; model/ramp 2.42×); check 3 amended to module-level imports; check 46 asserts holes and edge step. Frame-hash rule: the 14 preset × class renders byte-identical, `flat` = the 2026-09-14 field byte for byte. M3 Pro, 6 torch threads: CPU 0.27–0.31 s per image, load 1.2 s, MPS 0.06 s; CPU vs MPS disparity median 0, p99 0, max 2e-5, so no device flag. Real surface: 43 runs exit 0, endpoints 0 / 0; six mismatches at zoom 0.25: median displacement 26–46 % below flat (disparity 0.12–0.43), mean step 4–14 % lower, warping error equal or lower on five of six, `edge_ratio` ±0.06 except mismatch_2 0.19 → 0.24 and mismatch_6 0.53 → 0.59; correspondence 2.4–2.9 s vs 0.08–0.45 s; runs reproduce byte for byte. Matched: forced class B mean step 2.1–10.6 vs morph 0.5–5.3; research push-in `edge_ratio` 1.1–1.8 (sin(πu) steepest at endpoints, a defect). Gates: harness 82/82, transitions harness 51/51 in 9.6 s, both `check`s OK. Picks pending; class A semantics open. [revert: `--camera flat` default; revert commit + delete `models/depth/` and the manifest]
- **2026-09-26 — Owner picks on the camera sweep (0 of 12), and a retrospective in place of the next build: every slice since TR1 varied geometry, none varied the combination of A and B, which is what the owner grades; the basket cannot see it.** Owner (verbatim, with `benchmarks/runs/2026-09-23/picks.json`): "run was auwful , i feel like we are stuck , conceptually. I went ahead and repeated for every junky mismathing pair same logic with applied z depth pass, which was not expected to help with transition in any way and as i said can be at most nice additioal touch to image. Revisit full plan and my original goals , do additional deep reseatch, analyze in retrospect all recent runs and my answers and  see what is not working for us in our research and what prevents you from understanding my goals clearly and designing precise metrics to pursue." Picks: every mismatched pair `none` ("all are just unnecessary pans and basiacally cross fading between start and finish , not usable at all , no transition, that would preserve and gradually morph details"); matched pairs `morph_2s` only ("nothing got any  better than previous runs morphs"); mismatch_5 left empty; `matched_picks.json` not exported. Ingested into `benchmarks/2026-09-23-camera.md §5`; the 2026-09-23 handover's items 2 and 3 (the camera on matched pairs; `model` as the class B default) are closed as not wanted; `--camera` stays an option. Retrospective `.claude/claude-docs/audits/retrospective-2026-09-26.md`: (1) the goal in one paragraph from the owner's dated words, to be confirmed, with three failure modes (double exposure, decoration without transformation, invention); (2) six review rounds, about 340 clips, zero accepted, five rounds geometry-only, the alpha mix of `morph_frame` unchanged since 2026-09-13; (3) five method findings — built before a goal number existed, my eye stood in for the owner's, the owner's words were mapped onto inherited slice names, five rank revisions in ten days, a pick-shaped review page; (4) what I need from the owner: a reference clip, a yes/no on the paragraph, three per-property boxes per clip. Metrics: `scripts/research/verdict_metrics.py` (research only, imports neither tool) computes `feat_floor`, `laplace_floor`, `contrast_floor`, `motion_share`, `local_share`, `dissolve_fit` on a 640-px proxy of every graded clip (343) and scores AUC against hand-coded verdict labels (POS 35, XFADE 132, PAN 42, HALLUC 62). Pooled: the shipped basket 0.46–0.48 on POS vs XFADE (blind); `laplace_floor` 0.75 / 1.00 (vs PAN); `feat_floor` 0.69 / 0.79, and 0.73 against the generative clips; `dissolve_fit` 0.79 in the expected direction; `motion_share` 0.79 against the generative clips. Within-pair on match_1–5 (removes the related-vs-unrelated confound; 1–11 positives per pair): `laplace_floor` 0.78, `feat_floor` 0.76, `motion_share` 0.77, `dissolve_fit` 0.77 inverted, `local_share` 0.46 (the F2 candidate fails); the shipped `flicker.mean` 0.77 there because within a matched pair the picked clip is the one with more motion. No positive exists on any mismatched pair: nothing is calibrated where the problem is. Probe (`benchmarks/runs/2026-09-26/probe_mismatch_4/`): three auto-detected theme anchors (sun → sun, horizon → horizon) through the existing `--anchors` path on mismatch_4 — one sun travelling instead of two suns at mid-frame; `feat_floor` 0.17 → 0.28, `laplace_floor` 0.47 → 0.60, `motion_share` 0.39 → 0.60; the moved frame's edge shows at the left (backlog T16, the MLS twin of T13); three partition-mix variants score higher still (`feat_floor` 0.43–0.59) and read as blotchy wipes to my eye — "cheap effect on top" has no number yet. Research (primary sources; reports `benchmarks/runs/2026-09-26/research/track_{A,B,C}.md`): A — Regenerative Morphing (CVPR 2010) matches the goal paragraph (every frame from patches of the two photos, a disjoint term, "minimal ghosting" on unrelated pairs), no open-source implementation, patented to 2030, a large numpy port at an estimated 2–40 min per clip; Wolberg 1998: "Only after this alignment is maintained does a cross-dissolve … become meaningful"; B — DINOv2-S mutual nearest neighbours inside label-matched panoptic masks (Mask2Former-tiny) + YuNet faces + a brightest-blob sun ≈ 280 MB Apache-2.0/MIT, SPair PCK@0.1 55.6 for DINOv2-B, textureless regions need classical anchors; C — the morphing papers' metrics (PPL, PDV, LPIPS) pass a crossfade by construction; the dissolve-explained fraction and image-fusion metrics are the training-free candidates. Docs: `TRANSITIONS.md §11` (goal statement + metrics status), plan §Rank 2026-09-26 PROPOSED (goal paragraph → goal metrics into the basket → theme anchors hand-placed then Track B's recipe → the combination where nothing aligns → cameras and generative parked), backlog T16 + T17, audits README row, handover rewritten with the five questions. Gates: Gate 0 on six files; transitions harness 51/51 (9.7 s); both `check`s all OK; `harness.py` not run (no code changed; last 82/82 on 2026-09-23). Verification: real-pair-VERIFIED for the metric numbers and the probe (twelve fixtures' clips); the goal paragraph and every ranking below it are UNVERIFIED until the owner answers. [tier: Deep] [revert: n/a — record and proposal; the research script is outside both tools]
- **2026-09-26 — Owner answers to the retrospective: the goal paragraph is confirmed; F3 (invention) is refined; a second machine and generated keyframes enter the plan; the metrics build is ordered.** Owner (verbatim): "the goal paragraph is totally right, Imporatant to understand that we are trying to expand what is possible in general and check the frontier of all relevant computer  science approahes, math , computational and generative models and approaches that may help to achieve those mindboggling transitions that i would be able flexibly tune and control per scene. The goal is correct but important to remeber that you need to explore all cutting edge and non-obvious ways to push it even forward ( e.g - some very mismatched frames will inevitably require some sort of intermediate generation and interpolation ( otherwise it will again simply be boring crossfade) but real scientific problem is how to make that deterministic and fluent enough preserving start and end image details and creating seamless unexpected transition - maybe this will require some non-eucleadean geomentry , manipulating with colors and non obvious parts of the subjects/objects, maybe even some pseudo 3d-space manipulation , advanced flow techniques etc - these are not specific recommendations these are too give you additional insights how far  i want to push it! ) Also note we have another strix halo machine with 128GB ram  ( it is partiallt buse with my another important project but we can consider using it to test some local powerful imagegen  models like flux2 etc, maybe will help with intermediate frames ( full video generation is not desirable both because it is very computational intensive and wht whole point of transtion to to simply generate but be more of mix of scientific and deterministic ways and genertive helper elements. This may not even be a question of single midframe , for complex transitions we may need several so you can reliably interpolate later, and they should hold some ideal mix of start end but also depending on the scene contents they may get really inventive.   I have checked benchmarks/runs/2026-09-26/probe_mismatch_4/anchors_alpha/transition.mp4 - it still has  crossfade but it looks smoother now, i like how sun at least tries to be consistent and move more consistently bwtween frames . In more perfect result ( but again this is just for this specific scene! ) i would expect sun to morph to end sun , skyline (with z depth) and river morpth to end frame skyline and river independently but consistently . and clouds gradually shift into ( or nicely materilaize in the sky , so the scene basically got rebuild and transformed in real time, some 3d pans etc are just nice addition on top if relevant. Regarding Three failure modes : F1 and F2 - true , and as for `F3 invention` - not that simple - see my points above, it is more about where and how it is executed. Process averything i have wrtten above and see how it affects the plan, do additioal research if needed,  go ahead with the metrics. Also checked other anchors in probe_mismatch_4 , but except alpha that i mentioned above all others are junk , partion_dist, edge and luma ones create very harsh transitions with distinct ugly color borders (again see amy above thoughts, colors lumocity , transparency everytjing in scene should be natural ( maybe unexpected and impossible from human perspective  but still adhere to some higher order vector flow )". With one screenshot: the `partition_dist` mid frame of mismatch_4, a circular seam around the sun with a hard colour border. Effects: (1) the goal paragraph of the retrospective §1 is CONFIRMED and gains two sentences — the transition is tunable per scene, and colours, luminosity and transparency stay natural even where the content is impossible, adhering to one higher-order flow; (2) F3 is refined: invented content is allowed as a generative helper element when it is executed deterministically and fluently, preserves the start and end details, and is seamless; the rejection of 2026-09-22 stands for the per-frame SDEdit bridge (hallucination in every frame), not for generated KEYFRAMES that deterministic interpolation then bridges; full video generation stays out; (3) a second machine exists (a Strix Halo with 128 GB unified memory, partly in use) for local image-generation models such as FLUX.2, to make one or several intermediate keyframes per transition; (4) the ideal for mismatch_4 is a LAYERED transition: sun → sun, skyline with depth → skyline, river → river, each independently and consistently, clouds shifting or materialising, "the scene basically got rebuilt and transformed in real time", 3D pans as an addition; (5) the anchored alpha probe is graded "still a crossfade but smoother; the sun tries to be consistent"; the three partition variants are junk (hard colour borders), so a per-pixel switch without colour harmonisation is out; (6) "go ahead with the metrics": plan item 2 is ordered and built this session (next line). Plan §Rank 2026-09-26 moves from PROPOSED to RANKED with two new research tracks (generated keyframes on the second machine; layered / flow-based scene morphing with depth) before any build beyond the metrics. [tier: Quick] [revert: n/a — record]
- **2026-09-26 — Goal numbers built into the transitions basket: `assess()` gains `goal` (feat_floor, laplace_floor, contrast_floor, dissolve_fit, motion_share); harness 51 → 53; the sheet and the review page carry the numbers and three per-property boxes per clip; frames byte-identical.** Owner order (verbatim, 2026-09-26): "go ahead with the metrics". `transitions.py` Quality section: `goal_numbers(frames, cfg)` on the 480-px proxies (SIFT with `TCFG["goal_features"]` 1500 per endpoint, ratio test 0.75, matches to A over A's count plus matches to B over B's count, minimum over the interior; the Laplacian-variance and median 24-px patch-std ratios (`TCFG["goal_patch"]`) to the endpoints' interpolation, minimum; the mean R² of frame(t+1) − frame(t) ≈ β·(B − A); the mean share of the frame change that DIS flow explains); `report.json` `quality.goal`; `local_share` from the research script is not carried (within-pair AUC 0.46). Cost about 0.1 s per 8 proxy frames, about 1 s per 30. Harness section M: 52 (a plain crossfade of two unrelated harness scenes scores `dissolve_fit` 0.97 > 0.9, the aligned morph of `affine_pair()` 0.23 < 0.5; mutation "feed the crossfade's frames as the morph" → 0.97, red) and 53 (the aligned morph keeps `feat_floor` 0.97 > 0.5, a clip whose interior is a noise scene from neither photo reads 0.07 < 0.1, the CLI report carries the five keys; mutation "interior = A" → 1.13, red); both mutations applied in a throwaway copy on 2026-09-26: exactly 52 and 53 red, 51 passed; copy deleted. Not pinned on the synthetic pair: `laplace_floor` and `contrast_floor`, because on the harness textures the forward splat's resampling blurs the discs more than a blend of two unrelated textures does (aligned morph 0.45 against the crossfade's 0.85, the reverse of the real clips); their reference is the real-clip calibration (retrospective §5: AUC 0.75 / 0.67 pooled, 0.78 / 0.69 within pairs). Frame-hash recipe: the 14 preset × class hashes of `affine_pair()` and `blob_pair()` at 0.4 s / 20 fps are equal between the committed and the working module (`scratchpad/framehash.py`, HEAD copy imported by path). Real surface (`benchmarks/runs/2026-09-26/surface/` and `bench/`; morph 2 s, canvas ≤ 1920): match_4 (class A, 295 inliers) feat 0.420 · laplace 0.728 · contrast 0.809 · dissolve_fit 0.070 · motion −0.031, 10.6 s; mismatch_4 (class B flat) 0.085 · 0.455 · 0.806 · 0.195 · 0.139, 6.4 s; run 2 (through `scripts/bench_transitions.py`) reproduces run 1 to every digit on both pairs. Finding on the surface: `dissolve_fit` as built sees a static crossfade and not a panned one (mismatch_4's pan-and-zoom reads 0.195 although the owner calls the clip a crossfade); the motion-compensated form is the next candidate, not built. `scripts/bench_transitions.py`: rows carry `goal`, the sheet gains five columns, the page prints the numbers beside each clip and three checkboxes per clip (one picture · content transforms · nothing invented) whose state exports under `props[tag]` in `picks.json` and prints in the sheet's picks table; a preview `<pre>` shows what Export writes. Page verified statically (6 checkboxes on the two-pair bench page, the three keys, the preview element, the JS selectors match the markup); the click-through was NOT exercised (the Chrome extension was not connected this session): UNVERIFIED leg, named in the handover. Gates: Gate 0 on six files; transitions harness 53/53 (10.1 s); `transitions.py check` all OK; `harness.py` run alone after this line (result in the next line or the handover). Docs: `TRANSITIONS.md §2` item 6, §5 (53, section M), §6 (the surface rows), §11 (built); `AGENTS.md` roster 53; `contracts.md` (`quality.goal`, harness 52–53); `architecture.md` (map re-anchored 2026-09-26, the goal-number seam); plan item 2 BUILT; backlog T17 CLOSED (tails: motion-compensated dissolve fit, a seam number). harness-tested + real-pair-VERIFIED (match_4, mismatch_4). [tier: Standard] [revert: commit; frames byte-identical by the hash recipe, the report key is additive]
- **2026-09-26 — Owner answers: the second machine's docs named, a managed container route preferred, the theme anchors ordered.** Owner (verbatim): "1. Check this `/Users/yevhens/Projects/personal/epistemic-filter/deploy/README.md` and this `/Users/yevhens/Projects/personal/epistemic-filter/.serena/memories/project/machine_access.md` but also be sure to check readme: /Users/yevhens/Projects/personal/epistemic-filter/README.md, this is intricate production system with multiple jobs running there and  LLMs deployed in tricky balance , need to be very careful about resources sharing.  At the same time this machine has been used for various AI-related experiments , it has absolutely all drivers, stacks and various versions of tools installed to do any fine tuning and inference of a very wide range of text and image models ( mostly i used llama cpp, lmstudio , various flavors of comfy and swampui , and tried docker, ideally it should be some nice managed pre-build docker like lemonade and such but see what would be the most safe and efficient to use, idealy i wouldn't wnat to mess with host machine env and drivers too much. 2. Go on with theme anchors , feel free to explore". Read (the three files; facts in `mem:project/strix-halo-box`): the box is `ssh strix` (GMKtec EVO-X2, gfx1151, 128 GB, Ubuntu, ROCm, headless, passwordless sudo), a SHARED production LLM sidecar (llama-swap on Vulkan RADV keeps a 34 GB chat model and an 8B embedding model resident; free RAM 27.8–38 GB over 14 days) and another project's build box; it auto-suspends when idle (`touch ~/halo-hold` for a work session, remove after), reboots unconditionally Thu 05:00 BST, and that project's rule is "never global changes; everything user-scoped". On 2026-09-26 the box was unreachable from this Mac (the Tailscale name did not resolve, no 100.x address, the Tailscale CLI hung): nothing was probed or changed there. Research track F (`benchmarks/runs/2026-09-26/research/track_F.md`, primary sources): Lemonade (Apache-2.0, `ghcr.io/lemonade-sdk/lemonade-server`) does image editing but only with FLUX.2 klein 4B/9B and as a daemon; the least invasive route is stable-diffusion.cpp's Vulkan container (`ghcr.io/leejet/stable-diffusion.cpp`, 205 MB, pinned by digest; `/dev/dri` only, no ROCm, no `/dev/kfd`), which runs Qwen-Image-Edit-2511 GGUF (Q4_K_M 13.2 GB + Qwen2.5-VL-7B Q4_K_M 4.7 GB + mmproj 0.85 GB + VAE 0.25 GB = 19 GB, fits beside the LLMs), FLUX.2 klein 4B (8.9 GB GGUF) and FLUX.1 Kontext; a container `--memory` cap does NOT cover GPU (GTT) pages on this APU (ROCm/legacy-rocm-build#6370), so the protection is a pre-flight free-memory check, the quant/resolution budget and `--oom-score-adj 1000` on the job; ROCm tools overstate free memory on gfx1151 (ROCm#6004); every ComfyUI / PyTorch route needs `/dev/kfd` + seccomp changes and a 5–20 GB image, and the kyuz0 toolbox's host guide asks for kernel parameters and udev rules that the box's rules forbid. The one-shot recipe with caps, pre-flight, a sleep inhibitor and a manifest is `scripts/research/strix_keyframe.sh` (NOT RUN; needs the owner's go on a production box and Tailscale on the Mac). Anchors ordered: "Go on with theme anchors , feel free to explore" → the hand-placed page and the automatic probe are the next line. [tier: Quick] [revert: n/a — record]
- **2026-09-26 — Anchor border falloff built (`--anchor-falloff`, default 0 = off): the anchors path keeps the frame on the canvas; backlog T16 closed; harness 53 → 54.** Mechanism (measured on the first anchored renders of the six mismatches, `benchmarks/runs/2026-09-26/anchors/`): the Moving Least Squares field moves the whole frame, and where the moved frame no longer covers the canvas the coverage-aware mix switches to the other endpoint, so a moved frame edge walks through the picture (the anchors twin of T13 / T14; visible on mismatch_1, 2 and 5). Fix in the experimental lane: `TCFG["anchor_falloff"]` (0.0) and `anchor_falloff_max` (0.5), `TransitionSpec.anchor_falloff`, CLI `--anchor-falloff F`, `border_weight(h, w, F)` = a smoothstep from 0 at the canvas border to 1 at F × the short edge inward; `dense_displacement` multiplies the class B MLS field (method `mls-anchors+falloff`) or the class A anchor correction (`…+anchors+falloff`) by it and prints an INFO line; with F = 0 nothing is multiplied and the field is the 2026-09-13 one byte for byte. Harness section N, check 54: on two unrelated harness textures with three anchors that translate the whole frame by 120 px, the default field equals the plain MLS field exactly (method `mls-anchors`), at 0.2 the field is 0 on the four border lines (max 0.000 px), equal to the plain field at the centre (0.000 px), and the mid-frame hole fraction falls from 9.1 % to 0.00 %; mutation (a constant weight, applied by monkeypatching `border_weight` in a throwaway copy) → the border reads 120 px and the holes stay: 54 red, 53 passed; copy deleted. Frame-hash recipe: the 14 preset × class hashes equal HEAD's (`framehash.py`). Gates: Gate 0 on eight files, transitions harness 54/54, `transitions.py check` all OK. Docs: `TRANSITIONS.md §2.3/§3/§5`, `contracts.md`, `architecture.md` (map re-anchored, `border_weight`), `AGENTS.md` roster 54, backlog T16 CLOSED (the default stays 0 until the owner's boxes say the falloff reads better). Not built: a similarity / rigid MLS variant to stop the folds a three-anchor rotation produces (mismatch_2's face shears); noted for the layered design pass. [tier: Standard] [revert: `--anchor-falloff 0` is the default and byte-identical; a revert commit removes `border_weight`, the two config keys, the spec field, the CLI flag and harness N]
- **2026-09-26 — Falloff measured on the real pairs: a 0.2 band tears where the anchored field is large; 0.45 removes the frame edge on mismatch_1 without tearing; the page shows 0.45.** Renders `benchmarks/runs/2026-09-26/anchors/` (contact sheets `contact_four_variants_mid.jpg`, `contact_falloff_0_02_045.jpg`): at 0.2 the weighted field's gradient compresses content into streaks on mismatch_2 (a three-anchor rotation of about 200–400 px on a 602-px canvas) and in mismatch_1's sky; on mismatch_4 the automatic anchors (a wrong horizon row plus the sun) produce horizontal streaks over the whole frame at 0.2. At 0.45 mismatch_1 is clean (no edge, no tear) and mismatch_2 is a soft blend with striping in the trunk region, which is the fold of the MLS field itself, not the falloff. Check 54 pinned 0.2 on a synthetic 120-px translation, a smaller field than the real anchors produce; the check stands as the contract (border zero, centre unchanged, holes gone) and the real-pair guidance is 0.4–0.5 (`TRANSITIONS.md §2.3`). The theme-anchors page (`index.html`, four variants × six pairs where anchors exist, three boxes per clip) carries `anchors_falloff` and `anchors_auto` at 0.45. My eye (not evidence): `anchors_alpha` remains the best on mismatch_4 and mismatch_5, the falloff variants add nothing the owner asked for on the others, and the automatic anchors are junk on mismatch_1 and mismatch_4 (a bright cloud read as the sun; the strongest horizontal edge row is not the horizon). [tier: Quick] [revert: n/a — measurement; the option's default is 0]
- **2026-09-26 (late) — Owner: Tailscale enabled, the recommended box route approved in general; the box probed read-only through the prod box; its Tailscale is logged out.** Owner (verbatim): "enabled tailscale, machine should be reachable by now , check it, i am ok with your reccomended route in general. Prepare for next session, un-slop next session promt and actualize all docs". Reach: the Mac has a Tailscale address (100.107.57.13) but `ssh strix` and `ping` to the MagicDNS name time out because the box's own Tailscale is logged out (`tailscale status --self` on the box: "Logged out", with a login link the owner has to use); the prod box's documented wake script answered "halo awake and llama-swap healthy" and the jump path `ssh -J beelink yevhen@10.10.10.2` over the direct link works (host key accepted). Probe (read-only, 11:51 BST): Ubuntu 25.10, kernel 6.17.0-41, 32 threads, 121 GB RAM with 35 GB available (llama-swap's two `llama-server` processes at 20 + 10 GB RSS, `halo-poller`, Netdata running), GTT 123 GB total / 53 GB used, VRAM carve-out 2 GB, 499 GB disk free, `/dev/kfd` + `/dev/dri` with the user in `video` and `render`, ROCm 7.1.0 on the host, Vulkan 1.4.318 through RADV GFX1151 (Mesa 25.2.8), Docker 28.2.2 rootful with the user NOT in the `docker` group (podman present; distrobox / toolbox absent), Python 3.13.7, StabilityMatrix in the home, no `~/halo-hold`, `halo-idle.timer` every 5 min. Effects: track F's route stands with one adjustment — the one-shot runs as `sudo docker run … --user $(id -u):$(id -g)` (no persistent change) unless the owner adds the user to the `docker` group (a global change, the owner's call); the 19 GB GGUF set fits the 35 GB available with the pre-flight at 26 GB; the recipe `scripts/research/strix_keyframe.sh` is updated for sudo and stays NOT RUN until the next session with the owner present or an explicit go. Facts in `mem:project/strix-halo-box`. [tier: Quick] [revert: n/a — record; nothing changed on the box]
- **2026-09-26 (late) — Owner picks on the theme-anchors page: no variant is one picture or transforms content; the moved-frame edge is rejected wherever it shows; whole-frame rotations read as a "3D plane flip"; each pair asks for its own layered, orchestrated transformation.** File `benchmarks/runs/2026-09-26/anchors/theme_anchors_picks.json` (2026-09-26 11:21 UTC; ingested into the sheet and page). Owner (verbatim, with the picks): "added my picks json for the anchors run - read it carefully i tried to give detailed feedback and share my though process per pair ( but be carefull also not to generalize, you should see by now how it veries scenario per scenario)". Boxes: `not_invented` ticked on every deterministic clip that was graded (flat, anchors_alpha, anchors_falloff on mismatch_1, 4, 6; flat and falloff on mismatch_5); `one_picture` and `transforms` ticked NOWHERE — the first per-property signal: F1 and F2 fail on all 22 clips, F3 passes. Per pair, verbatim: mismatch_1 → anchors_falloff: "anchors_falloff at least tries continuous transform although it mostly just smudges image A until it is closer to B and then cross fades ( but it is a bit less obvious). flat - would be nice pan and zoom if any transformation existed in between , otherwise now it is just crossfade between A(zoom-in) and B(zoom-out) . anchors_alpha does funny image rotation according to some detected features per your legend above i guess, but because minor details ( parts of buildings , clouds etc ) are not transforming enough it looks like one picture rotates ( with hard ugly visible frame edges by the way ) and then cross fades into B, still idea is iterensting , this is unpredictable motion , and if clouds and buildings could cleanly and interestingly morph there, this could be one of the options."; mismatch_2 → none: "all anchors ( expect of flat which is similar to previous runs, boring cross fade) produce ugly image plane 3d flip effect  across diagonal axes and with crossfade as well, maybe because of severe mismatch between A and B and difficulties to find any proper anchors"; mismatch_3 → anchors_falloff: "In general , same issues as in `mismatch_1` pair, but here mbecause it is multiple people and closer locations effect are different, i marked `anchors_falloff` ( also anchors_auto here is simialor in idea)  but they are far from perfect and still mostly smudging of A to some degree of B and them crossfading, but i like how it at least ( very poorly ) attempts to anchor it to something in target image , for this scene i would expect for experiments with depth and parts of background behind group of people transforming independently from people and people in foreground doing same + colors objects etc shift in place ( i don't mind some weird swirling or rotation or other translational movents but it should make sence for final image and be coherent"; mismatch_4 → anchors_alpha: "In general , same issues as in `mismatch_1` pair, i marked `anchors_alpha` but it is far from perfect, still crossfadeing and not enough landscape transformation,  but i like how it at least ( very poorly ) attempts to anchor sun to itself in target image . It is almost same as `benchmarks/runs/2026-09-26/probe_mismatch_4/anchors_alpha/transition.mp4` that i did feedback before. ALso `anchors_auto` here is really broken , image B just pulls from below in ugly distorted way"; mismatch_5 → none: "flat is similar to previous runs, boring cross fade),  anchors_alpha here   produce ugly image plane 3d flip effect  across diagonal axis ( maybe because of meteor streak across frame in B which got picked as feature)  and with crossfade as well, maybe because of severe mismatch between A and B and difficulties to find any proper anchors . anchors_fallof - same as anchors_alpha but lesser degree"; mismatch_6 → anchors_falloff: "flat is similar to previous runs, cross-fade with subtle pans, in this case not really bad, but pretty generic  ( still cross fade IS bad.) Other variats mostly similar to flat with different small distortions , not changing final flow much for me , nothing interesting , and no content transforamtion, again here maybe because it is harsh day to night and to a scene with different sky and foreground features. I would expect here for clouds to interestingly dissolve into dark, backgorund sky darken independently , then stars appear , buildings in bootom go down, and foreground elements ( like trees and overhanging air conditioning unit ) enter the frame . Maybe all of this with some zoom out , rotation and/or parallax on cloud / buildings - these are just examples, there are endless scenarios how this transition could be orchestrated." Corrections of fact for the record: on mismatch_5 the diagonal rotation was MY hand-placed anchors (the bridge ends mapped to the galaxy disk), not a detected streak; on mismatch_4 the broken `anchors_auto` is the strongest-horizontal-edge "horizon" rows (303 / 273) that are not the horizon. Reading, pair by pair, no generalisation beyond what each note says: (1) a whole-frame warp from a few anchors (rotation, affine) reads as "one picture rotates" or a "3D plane flip" and is rejected on mismatch_1, 2 and 5; on mismatch_1 the owner keeps the idea IF the parts (clouds, buildings) morph locally and cleanly; (2) the frame edge is "hard ugly" (mismatch_1) — the falloff is preferred over the plain anchors on 1, 3 and 6, the plain anchors on 4 where the sun landing is what counts; (3) mismatch_3 asks for depth layers: background behind the people and the people in front transforming independently, colours and objects shifting in place, swirls or rotations allowed if the end image stays coherent; (4) mismatch_6 gives a full orchestration in time: clouds dissolve into dark, the background sky darkens on its own, then stars appear, the buildings go down and out, the trees and the air-conditioning unit enter, with an optional zoom-out, rotation or parallax — "endless scenarios how this transition could be orchestrated"; (5) mismatch_2 and 5 have no proper anchors at all ("severe mismatch"): these are the pairs for generated keyframes or an orchestration that moves objects in and out rather than a warp. Effects: plan item 3 becomes the layered, ORCHESTRATED transition — layers (depth, sky / clouds / stars / buildings / trees / people), an action and a time window per layer (darken, dissolve, materialise, exit, enter, move, hold), a per-layer field with anchors only inside a layer, never a whole-frame rotation, a per-layer colour path, compositing by depth — with a per-scene score the owner can edit (the "tune and control per scene" of the goal paragraph); the first probe is mismatch_6 exactly as the owner scored it, then mismatch_4 (sun, skyline, river) and mismatch_3 (depth layers). The anchors' default falloff: decided below (next line). Sheet: `benchmarks/runs/2026-09-26/anchors/sheet.md §Owner picks`. [tier: Quick] [revert: n/a — record]
- **2026-09-26 (late) — The anchors' default border falloff is 0.45 (`TCFG["anchor_falloff"]`); `--anchor-falloff 0` restores the plain MLS field; harness 16 and 54 follow.** Owner (verbatim): "3. up to you to decide". Evidence: the owner's picks of the same day prefer `anchors_falloff` over `anchors_alpha` on mismatch_1, 3 and 6 and call the plain field's frame edge "hard ugly"; the plain anchors won only on mismatch_4, where the sun landing decides and the falloff variant changed little; a 0.2 band tears, 0.45 does not (the previous two lines). A `TCFG` default is not the experimental lane, hence this line. Scope: only the anchors path changes (class B `mls-anchors` → `mls-anchors+falloff` by default; the class A anchor correction likewise); the default path without anchors is untouched — the 14 preset × class frame hashes equal HEAD's. Harness: 16 now expects `mls-anchors+falloff` for anchors under the default; 54 pins that `--anchor-falloff 0` equals `mls_affine` byte for byte with method `mls-anchors`, that the default gives `mls-anchors+falloff` with 0.00 % mid-frame holes, and keeps the 0.2 border / centre / hole assertions with their mutation; 54/54. `transitions.py check` all OK. Docs: `TRANSITIONS.md §2.3/§3`, `contracts.md`, backlog T16, the handover watch. [tier: Standard] [revert: set `TCFG["anchor_falloff"]` back to 0.0 and restore checks 16/54 to the 2026-09-26 afternoon form (one commit)]
- **2026-09-26 (late) — The box is back on Tailscale; `ssh strix` works from the Mac; the hold file is removed.** Owner (verbatim): "wake up halo box so i cna login from it" · "login done, i am able to directly  access". Done: the prod box's documented wake script (`~/epistemic-filter/deploy/halo/wake-halo.sh`) answered "halo awake and llama-swap healthy"; `~/halo-hold` was set at 12:37 BST so the idle timer would not suspend the box during the login and removed at 12:46 after the owner's confirmation; verified from the Mac over Tailscale: node 100.119.4.32, `llama-swap` and `halo-poller` active, 35 GB available. Nothing else changed on the box. The handover's box watch now tries `ssh strix` first and the jump path second. [tier: Quick] [revert: n/a]
- **2026-09-26 (third session) — The orchestrated layered transition is designed (`TRANSITIONS.md §12`) and probed on three mismatched pairs from hand-written per-scene scores; nothing in `transitions.py` changed.** Specification: the owner's mismatch_6 sequence and mismatch_3 note (verbatim in the "Owner picks on the theme-anchors page" line above) and the mismatch_4 sentence (plan §Rank 2026-09-26 item 1). Built: `scripts/research/layered_probe.py` (a score = layers × one action each × a time window; mask rules on the offline depth model and the photo: `depth_fg/bg` with `dilate`, `refine: dark`, `grow`, `components`; a per-column `skyline` from the depth with a texture veto and hand polygons; `band_dark` with `above_band` / `below_band`; `depth_band`; `clouds` by the blue or the brightness of the sky; `stars`; `sun` with Gaussian moments; actions `backdrop` (per-row Lab fit A → B, residual crossfade), `hold`, `recolor`, `dissolve` / `materialise` by density, blobs, luma, rows or noise, `exit` / `enter` / `move`, `move_to` by the closed-form Bures–Wasserstein map; per-layer per-row colour path; over-compositing back to front; frame 0 = A and the last frame = B exactly; the tool's encoder, canvas and quality basket reused) and the tracked scores `scripts/research/scores/{mismatch_6,mismatch_6_overlap,mismatch_4,mismatch_3}.json`; page `benchmarks/runs/2026-09-26/layered/index.html` (each probe beside the two clips the owner graded on the anchors page, three boxes per clip, a pick and a note per pair, `layered_picks.json` export); sheet `.claude/claude-docs/benchmarks/2026-09-26-layered.md`. Numbers (2 s, canvas ≤ 1920 px, 480-px proxy): mismatch_6 `layered` first / last interior step 0.05 / 0.05 levels, `edge_ratio` 0.0105, feat 0.055, laplace 0.241, contrast 0.197, dissolve_fit 0.226, motion 0.290, 9.4 s wall (the depth model 1.3 s per photo, render 5.8 s for 60 frames at 1146×1524); `layered_overlap` 0.06 / 0.05, 0.0132; mismatch_4 `layered` (sun moved by the Gaussian map, scale 0.86 × 0.83, translation (156, 271) px) 0.11 / 0.23 levels, `edge_ratio` 0.188, feat 0.108, laplace 0.518, contrast 0.713; mismatch_3 `layered` (three depth bands per photo, noise dissolves with 4–8 % zoom) 0.14 / 0.00, `edge_ratio` 0.071, feat 0.445, laplace 0.914, contrast 0.949; two runs byte-identical (md5 `64a54eee839d72d195b04d9dc6a06c1f`, `1d2e8bbbbbaa829c908985c101fb63a4`). The goal numbers were calibrated on crossfade-versus-closer verdicts and do not see an orchestration; the low floors on mismatch_6 measure the smooth sky the owner's sequence asks for. Defects by my eye (§12.3; not evidence of quality): the depth model's mattes fail at edges (leaf tips at disparity 0, a dome above a roof, the far tower dropped: backlog T18), the moved sun leaves its wider glow behind and B's glow fades in early, the sinking skyline of mismatch_4 streaks at the left shore, mismatch_3 is a depth-ordered patch reveal. Four probe bugs fixed and recorded as traps (the cosine ease outside 0..1; the entry travel; the texture skyline; the residual ghost under a soft edge). Rule kept: nothing enters `transitions.py` before the owner grades a probe "one picture" or "content transforms"; the shape then is `pair --score FILE` (§12.4). Gates: Gate 0 clean (the four tool files + the probe), `harness.py` 82/82, transitions harness 54/54, both `check`s OK; `git diff` on `transitions.py` empty, so the 14 frame hashes are HEAD's by construction. Verification: harness-tested (gates); real-pair rendered on three catalogued pairs; UNVERIFIED as quality until the owner's boxes. Docs: `TRANSITIONS.md §6` row and §12, plan §Rank item 3 progress + change log, backlog T18, the sheet, `mem:core`, `mem:project/wip-2026-09-26-layered-transition`. [tier: Deep] [revert: delete `scripts/research/layered_probe.py`, `scripts/research/scores/`, `TRANSITIONS.md §12` and its §6 row, the sheet; `transitions.py` untouched]
- **2026-09-26 (third session, late) — Owner picks on the layered page: the two mismatch_6 layered clips are the first on record graded "content transforms"; "one picture" on none; the noise-ordered patch dissolves of mismatch_4 and mismatch_3 are rejected.** File `benchmarks/runs/2026-09-26/layered/layered_picks.json` (2026-09-26 13:19 UTC; ingested into the page and both sheets). Owner (verbatim): "picks exported , prepare for next session". Boxes: mismatch_6 `layered` and `layered_overlap` = transforms + not_invented; `flat` and `anchors_falloff` = not_invented; mismatch_4 `anchors_alpha` and `flat` = not_invented, `layered` no box; mismatch_3 `anchors_falloff` and `flat` = not_invented, `layered` no box; `one_picture` nowhere. Closer: mismatch_6 none, mismatch_4 `anchors_alpha`, mismatch_3 none. Notes (verbatim): mismatch_6 — "anchors_falloff and flat more or less same as in previous runs ( at least not worse ), layered variants added intneresting movements of clouds / front objects but they leave a lot of countours , afterimages and in general feel too direct and rushed , but i like how  you at least was able to isolate some objects and themes in layered variants, still very junky and crossfade is present in all variants ( although in anchors_falloff it is  rather subtle )"; mismatch_4 — "anchors feel more or less same as in previous runs ( at least not worse, somewhat smooth, still crossfade present ), layered variants in this example are very juny patchy dirty transitions , not useful at all , i feel like this is because for this example luma and colors vary between frames and also lots of distinct edges "; mismatch_3 — "anchors feel more or less same as in previous runs ( at least not worse, still crossfade present ), layered variants in this example are very juny patchy dirty transitions , not useful at all , i feel like this is because for this example luma and colors vary between frames and also lots of distinct edges ". Reading, pair by pair, no generalisation beyond each note (`TRANSITIONS.md §12.6`): (1) mismatch_6: the first "content transforms" ticks on record (0 of 22 clips on the anchors page, none earlier); the faults named are contours, afterimages, a direct and rushed pace, and a crossfade in every variant; my mapping of each onto the probe's parts is [INFERRED] (matte edges; what stays behind a moving layer; a 2 s clip with sequential eased actions; the backdrop's residual crossfade and the clouds' recolour); (2) mismatch_4 and mismatch_3: noise-ordered dissolves and materialisations are rejected where a layer has internal structure, with the owner's cause (luma and colour varying between frames across many distinct edges); the mismatch_3 depth-band family is out; mismatch_4's next probe keeps the sun move and the two backdrops with a clean slide of the skyline band. Effects: the §12.4 gate ("graded one picture or content transforms") is met on its letter by mismatch_6 `layered` while the same note grades it "very junky"; recommendation, for the owner to confirm or override: no tool build yet, one more probe round on mismatch_6 aimed at "one picture" (remove the residual crossfade, the recolour during erosion, the matte contours and the afterimages; 3–4 s with overlapping windows), then re-grade. Docs: sheet §Owner picks, `TRANSITIONS.md §12.6`, plan §Rank item 3, backlog T18 state, `mem:core`, the leaf, handover. [tier: Quick] [revert: n/a — record]
- **2026-09-26 (third session, last) — Owner orders for the next session: the box run is scheduled without the owner present; round 2 on mismatch_6 targets the three afterimages the owner names, at 3 s; more work per session with Opus 5.5 subagents.** Owner (verbatim): "schedule the box run for next session as well , do not wait for me , you have all you need to reach it , just be careful with resources, everything else -per your plan and reccomendations, try to fit more work into next session , use opus 5.5  suagents where needed. Regarding Two questions would steer round 2: 1) i see afterimage for tree crown, distinct clouds, bottom buildings contours 2) keep 3s for now". Effects: (1) supersedes "only with the owner reachable" for plan item 4: the next session runs `scripts/research/strix_keyframe.sh` on the box on its own, under the resource rules of `mem:project/strix-halo-box` (the pre-flight refuses below 26 GB available, `touch ~/halo-hold` for the session and remove it after, `sudo docker run … --user`, no host change, one-shot with caps, the two sha256 sums compared); (2) the recommendation stands as the plan: no tool build yet, round 2 on mismatch_6 aimed at "one picture" with the three named afterimages as its targets — the tree crown (the leaf tips outside the depth matte ghost in the sky residual; the `grow` band), the distinct clouds (the cloud fringe left in the backdrop's residual while the clouds erode, and their recolour), the bottom buildings' contours (the skyline cut and the sky fit left under the exiting buildings) — at 3 s, windows overlapping where the sequence allows; (3) mismatch_4 round 2 (the sun move and the backdrops with a sliding skyline, no patches) if time; (4) per-model routing recorded in the handover: Opus 5.5 subagents for the box track (long, remote) and the DECISIONS compaction (mechanical, byte-verbatim with an md5 receipt), the main thread on the probe; waves of at most three tracks. Docs: handover, plan items 3–4, `TRANSITIONS.md §12.6`, `mem:core`, the leaf. [tier: Quick] [revert: n/a — record]
- **2026-09-26 (fourth session) — `DECISIONS.md` compacted: the era 2026-09-13 → 2026-09-23 (37 lines, 69,832 bytes, md5 `70a09c0ee2da631f62cee0f701c0ea9d`) moved byte-verbatim to `DECISIONS_ARCHIVE.md` under `## Moved 2026-09-26`; a 23-line digest (22,612 bytes) stands in its place; the file is 71,282 bytes (was 118,351).** Done by an Opus 5.5 subagent (Track C of the handover) and verified on the main thread: the block cut back out of the archive has the same md5; lines 1–22, 23–41 and 79–91 of the old file are byte-identical to lines 1–22, 24–42 and the last 13 lines of the new one; the list of dated lines (57 before, 20 after) equals the old list minus the 37 moved lines; the Compactions table holds row 1. The subagent compared the 432 distinct numbers of the moved block with the digest: 7 were missing and were added, 6 apparent misses were substrings (for example "48" inside "480"). The brief said the era's harness count ends at 53; the moved lines say 36 → 38 → 40 → 43 → 51 (53 is a 2026-09-26 line), and the digest records the moved lines' sequence. The digest passed the no-slop pass with no number removed. [tier: Standard] [revert: `git revert` of this commit restores both files; the archive block is byte-verbatim, so a reverse move is lossless]
- **2026-09-26 (fourth session) — Round 2 of the layered probe on mismatch_6 at 3 s: three clips on `benchmarks/runs/2026-09-26/layered_r2/index.html` beside round 1's `layered` and `flat`, one score with the owner's three afterimages addressed at their source; nothing in `transitions.py` changed; the owner grades next.** Owner (verbatim, 2026-09-26): "1) i see afterimage for tree crown, distinct clouds, bottom buildings contours 2) keep 3s for now". Diagnosis on round 1's composite, measured (`TRANSITIONS.md §12.7`): the cloud rings at t = 0.5 are the cloud fringe (matte alpha under 0.9) left in A's sky residual at weight 0.21; the light band above the roofs and the light rectangle are the per-row day-sky fit extrapolated under the exiting buildings, and the round-1 "far tower" polygon (604–691 × 1177–1326 px) encloses sky and cloud, not a tower (L 66.7–67.3 across its top edge, no step); the depth skyline sat 6–43 px above the roof edges (cloud edges under 6 L per px, roof edges 5–17); at t = 0.76 the leaf tips and an isolated branch (560–600 × 1180–1240 px) sat in the sky at their final position because B's residual excluded only the interior of a leaf-level matte, while the sky layer itself equals B's fit plus residual to 0.28 levels. New keys in `scripts/research/layered_probe.py`, every one default-off (round 1's score still renders md5 `64a54eee839d72d195b04d9dc6a06c1f` through the changed probe): `region_of` (a 40-px-dilated, 25-px-soft region around a tight core, so the trees and the AC unit carry B's own sky between the leaves), `refine: dark_or_chroma` + `chroma_d` 10 + `min_blob` 400 + `components_bridge` 60 inside a 220-px depth prior (lit leaves and isolated branches join the core, stars do not), `snap_px` 45 / `snap_up` 8 / `snap_min` 4.5 (the skyline moves to the topmost luminance step; 1,104 of 1,146 columns), full-alpha `minus` / `exclude` entries and residual-only `minus_res` / `exclude_res`, `fill_sigma` 60 (a normalized-convolution fill of the residual holes at 60 / 180 / 540 px blended by confidence, at quarter resolution), `fit2d` (a per-band quadratic in x, so B's lateral glow is fit, not residual), `recolor_mode: "L"`, `grow` on every rule, and `fit_under` (built, not used: the vacated band continues the interpolating sky). Tried on the frames and rejected: a semi-transparent cloud matte (`opaque` 0.5: frame 1 differed from A by 4.28 levels), a global quadratic hole fill (a dark crown-shaped hole), a fill switching scales at a threshold (an iso-distance contour), the wide region as the residual hole (a 600-px hole shows its outline whatever fills it), the 120-px prior with a plain bottom-component rule (leaves floated). Scores `scripts/research/scores/mismatch_6_r2{,_L,_drift}.json`: 90 frames; clouds 0.05–0.52 opaque wherever density exists (`opaque` 0.01, `feather` 0.1, thin-first), sky 0–0.70, buildings 0.30–0.75 with `grow` 2, stars 0.45–0.90 outside the wide regions, trees 0.55–0.95, AC unit 0.60–0.97, `ease` on every layer; `_L` darkens the clouds in luminance only toward the night sky; `_drift` drifts them 4 % right and zooms 4 %. Numbers (1146×1524, 480-px proxy): first / last interior step 0.28 / 0.05 levels on all three (round 1: 0.05 / 0.05; the 0.28 is the 4–8 px band above the roofline and the cloud sliver where the residual is excluded), `edge_ratio` 0.097 / 0.0965 / 0.0934, feat_floor 0.0 (the empty smooth sky between the clouds' exit and the trees' entry; the goal numbers do not see an orchestration, §12.2), laplace_floor 0.099, contrast_floor 0.118, dissolve_fit 0.054 / 0.211 / 0.050, motion_share 0.290 / 0.317 / 0.311, prep 3.4–5.0 s, render 7.2–8.0 s; two runs byte-identical (md5 `cddde79e…`, `f3efeeef…`, `7934592a…`). Defects still visible by my eye (not evidence): the last cloud cores vanish as bright blobs on a dusk sky around t = 0.45–0.52; the sky under the right-hand trees reads darker at the bottom-right corner before the trees arrive; the AC unit's region enters as a hard-edged wedge. Gates: Gate 0 clean (the four tool files, the probe, the bench), `harness.py` 82/82 in 43.5 s alone, transitions harness 54/54, both `check`s OK; `transitions.py` byte-identical to HEAD, so the 14 frame hashes are HEAD's by construction. Docs: `TRANSITIONS.md §6` row and §12.7, sheet `benchmarks/2026-09-26-layered.md §Round 2`, plan item 3 and change log, backlog T18, `mem:project/wip-2026-09-26-round2-box-compaction`. [tier: Standard] [revert: delete the three `_r2` scores and `git revert` the probe edit; `transitions.py` untouched]
- **2026-09-26 (fourth session) — Track D: mismatch_4 round 2 at 3 s on the same page (`benchmarks/runs/2026-09-26/layered_r2/`, section mismatch_4, beside `flat` and `anchors_alpha`): the sun disc moves to the second sun's place and the second sun takes over on arrival, A's skyline band with its masts slides down as a whole behind the water, B's band rises behind it, the waterline slides from one shore to the other, no cloud layer and no noise-ordered patch; ungraded.** Owner's round-1 verdict (verbatim, 2026-09-26): "layered variants in this example are very juny patchy dirty transitions , not useful at all , i feel like this is because for this example luma and colors vary between frames and also lots of distinct edges". Score `scripts/research/scores/mismatch_4_r2.json` (15 layers, 7 rendered): `band` = `band_dark` (median 31, the bottom capped at 0.56 of the height so the dark water reflection at the left shore stays water) ∪ `silh_a` (`dark_in` relative to the local sky brightness over 120 px, darkness 0.35–0.6, blobs of 40 px or more, inside the sky above the band), `exit` down 0.15–0.70; `band_b` the same on B (cap 0.75), `enter` from below 0.30–0.85; `sun` (the disc + 6 px) `move_to` `sun_b` 0.15–0.70 with the colour path; `sun_b_layer` materialises by luma 0.68–0.80; `sky` backdrop 0–0.85 with `fit2d` and `fill_sigma` 60, the suns' interiors out of both residuals, the bands' full alpha out of the residuals only; `water` backdrop 0.10–0.85 with `slide_between` the two bands' bottom lines. New probe keys, default-off: `slide_between` + `slide_feather`, `dark_in` (absolute `L`, or `relative: "local"` with `local_px`, `dark_lo`, `dark_hi`; `min_blob`, `rows`, `within`), `minus_mask`, `or` on any mask rule, `max_bottom` on `band_dark`. Tried on the frames and rejected: B's clouds as a layer materialising by L density with no noise (the L-density cloud rule takes the sun's glow for cloud on a sunset sky, the fit under it fell to L 24 at the sun and a dark disc showed at the sun's target; the clouds now arrive with B's sky texture); the suns carrying 40–80 px of glow with full-alpha exclusions (a rounded patch of filled residual around the sun at t = 0.5 and 0.75; frame 88 differed from B by 44 levels inside the sun region before the late B sun layer); an absolute luminance threshold for the silhouettes (B's cranes L 6–25 against its dim sky at 18–30; the local relative darkness separates them: silhouettes 0.2 % of A and 2.1 % of B); a `move` of 560 px (the left buildings stayed above the left shore's waterline; `exit` travels 1,002 px). Numbers (1920×1092, 480-px proxy): first / last interior step 0.12 / 0.10 levels (round 1: 0.11 / 0.23), `edge_ratio` 0.172 (round 1: 0.188; 1.22 with the cloud layer), feat_floor 0.155, laplace_floor 0.636, contrast_floor 0.844, dissolve_fit 0.175, motion_share 0.078, prep 4.3 s, render 9.6 s; two runs byte-identical (md5 `b7700b4c5c2ae971bb5e2505070306ad`); round 1's mismatch_6 score still renders `64a54eee…` and the mismatch_6 round-2 score `cddde79e…` through the final probe. Defects still visible by my eye (not evidence): a faint disc-shaped patch at the second sun's place from t ≈ 0.4 until the moved sun arrives (the filled hole of the excluded disc); the sky behind A's left buildings is a flat extrapolation until B's sky takes over; A's water reflections stay as stripes at the old waterline while it slides; a horizontal cut at the far-left structure's top as B's band rises. Docs: `TRANSITIONS.md §12.7` (mismatch_4 paragraph and row), the sheet, plan item 3. [tier: Standard] [revert: delete `scripts/research/scores/mismatch_4_r2.json`; the probe keys are default-off; `transitions.py` untouched]
- **2026-09-26 (fourth session) — Track B: the keyframe recipe ran twice on the box without the owner and the two keyframes are byte-identical (sha256 `c8d6f50f391b835df085bb72d12dd96eec4ae5a726bcf5356ef16eab5c5f1573`); the keyframe reproduces image 2 rather than a midpoint; the box is clean (hold file removed, no container, 36 GB available).** Owner (verbatim, 2026-09-26): "schedule the box run for next session as well , do not wait for me , you have all you need to reach it , just be careful with resources". Run by an Opus 5.5 subagent under the handover's resource rules; the numbers below were read from the logs copied to `benchmarks/runs/2026-09-26/keyframes/mismatch_4/` and re-checked on the main thread (`shasum`, `cmp`, the memory samplers). Reach: `ssh strix` direct. Pull: 13 s (image ID a9e7afd91eba, 568 MB unpacked; the one host-side residue). Models: 19,034,757,094 B in 373 s (51.0 MB/s); the four file names equal the script's; sizes and sha256 equal Hugging Face's (table in `mem:project/strix-halo-box`). First attempt: failed in 0 s at `systemd-inhibit` ("Failed to inhibit: Interactive authentication required." over a non-interactive ssh session); the subagent stopped and cleaned up as the rules say; the recipe was patched on the main thread to probe the inhibitor once and run without it when refused (the idle suspend is guarded by `~/halo-hold`, which the run touches), with `W` / `H` / `MAX_VRAM` / `MEM_CAP` environment overrides at the recorded defaults; the subagent resumed. Run 1 (kf_01: 1344×768 to keep the canvas ratio, seed 20260926, cfg 2.5, euler, flow-shift 3, 20 steps, `--max-vram 20`, `--memory 12g`, both canvas frames of mismatch_4 as references, the prompt in the manifest): wall 1,804 s, sampling 1,779 s (81–103 s per step), VAE decode 8.1 s; "total params memory size = 17720.80MB (VRAM 17720.80MB, RAM 0.00MB)"; MemAvailable 37.97 → 15.46 GB, GTT used 56.5 → 77.4 GB; a "[WARN] auto-fit: no GPU devices; using the default backend" line despite the RADV device (its meaning UNVERIFIED; the weights still report as VRAM). Run 2 (kf_01b, identical arguments): wall 1,941 s, sampling 1,916 s, decode 8.11 s, MemAvailable min 15.07 GB, GTT max 72.30 GB; the manifests are equal except the date; the PNGs are byte-identical (`cmp`). The keyframe, measured on the canvas: sun centroid (967, 459) against B's (962, 460) and A's (941, 225); mean absolute difference 4.8 levels to B and 33.1 to A; Laplacian variance 188 on the 480-px proxy against B 254 and A 198. The model returned image 2 at lower detail, not the midpoint the prompt asked for; the owner's eye grades it on `benchmarks/runs/2026-09-26/keyframes/index.html` (before, kf_01, after side by side). Left on the box for the next run: `~/keyframes` (18 GB) and the image; nothing installed, no host change. Docs: plan item 4, `mem:project/strix-halo-box`. [tier: Standard] [revert: n/a for a run; the recipe patch is a `git revert`; `~/keyframes` and the image on the box can be removed by hand]
- **2026-09-26 (fourth session) — Units correction to the Track B line above: the MemAvailable figures there were read as kB / 10⁶ and labelled GB; `/proc/meminfo` reports KiB, so in GiB they are 36.2 → 14.75 (run 1, a drop of 21.46 GiB) and a minimum of 15.07 (run 2, a drop of 20.83 GiB); the GTT figures are decimal GB as written (56.5 → 77.4 GB run 1, 72.3 GB run 2, rises of 19.47 and 19.34 GiB).** Two facts from the subagent's final report, read from the box: no OOM line in the kernel log during either run and both resident `llama-server` processes kept their morning start times; one kernel line `amdgpu: Fence fallback timer expired on ring comp_1.1.0` fell at 15:15:56 during run 1, and the same message appears on 24 and 25 September before any keyframe job, so the job is not shown to have caused it. [tier: Quick] [revert: n/a — record]
