# Audits
Reports from `/reveal` Audit sessions (`.claude/skills/reveal/references/audit-mode.md`). One file
per run: `audit-<scope>-<YYYY-MM-DD>.md`. Read-only sessions; fixes are sliced into later sessions.
First audit: Track D + F at the first phase boundary (after slices H1–H3 land) — backlog row T1.
| Date | Scope | Findings (B/M/m/n) | Slices opened | Baseline anchor |
|---|---|---|---|---|
